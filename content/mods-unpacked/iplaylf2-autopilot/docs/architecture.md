# Autopilot 架构

本文面向 Autopilot 维护者，定义运行链路、公共观察、机制语义和滚动规划。安装与启用说明见
[README](../README.md)；允许使用的信息和控制能力由 [玩家权限边界](fair-play.md) 规定，本文不重复
权限条款；目录归属、依赖方向和公共入口见 [模块边界与责任](module-boundaries.md)。

按维护任务选择阅读入口：

| 任务 | 首要入口 | 配套文档 |
| --- | --- | --- |
| 修改公共数据或记忆语义 | [观察契约](#观察契约) | [玩家权限边界](fair-play.md) |
| 接入版本机制 | [机制知识](#机制知识) | [敌人、树木与投射物参考](vanilla-enemy-mechanics.md)、[玩家效果与武器机制审计](vanilla-item-weapon-mechanics.md) |
| 修改死亡奖励、掉落概率或奖励条件 | [玩家效果规则](#玩家效果规则)、[动态效用](#动态效用)、[武器结果预测](#武器结果预测) | [敌人完成价值与死亡奖励](model-calibration.md#敌人完成价值与死亡奖励)、[玩家效果与武器审计](vanilla-item-weapon-mechanics.md)、[敌人、树木与投射物参考](vanilla-enemy-mechanics.md)、[模块边界](module-boundaries.md#机会规则与动作结果) |
| 修改决策行为或诊断字段 | [滚动规划](#滚动规划) | [战斗采样与模型校准](model-calibration.md) |
| 修改材料收集或拾取路径 | [决策基底](#决策基底)、[暴露与导航意图](#暴露与导航意图) | [导航与材料](model-calibration.md#导航与材料)、[模块边界](module-boundaries.md#机会规则与动作结果) |
| 修改消耗品恢复、伤害或拾取规则 | [观察契约](#观察契约)、[玩家效果规则](#玩家效果规则)、[动态效用](#动态效用) | [拾取规则事件](model-calibration.md#拾取规则事件)、[模块边界](module-boundaries.md#机会规则与动作结果) |
| 修改环境暴露、碰撞几何或承伤换算 | [暴露与导航意图](#暴露与导航意图)、[结果与诊断](#结果与诊断)、[动态效用](#动态效用) | [运动、碰撞与生命](module-boundaries.md#运动碰撞与生命)、[敌人、树木与投射物参考](vanilla-enemy-mechanics.md)、[生存与碰撞](model-calibration.md#生存与碰撞) |
| 修改敌对/友方划分或转化单位处理 | [友方角色与构筑物](#友方角色与构筑物)、[敌人行为画像](#敌人行为画像) | [敌人与投射物](vanilla-enemy-mechanics.md#敌人与投射物)、[观察与记忆](module-boundaries.md#观察与记忆) |
| 修改可交战目标或树木完成机制 | [暴露与导航意图](#暴露与导航意图)、[武器结果预测](#武器结果预测) | [可交战目标与武器结果](model-calibration.md#可交战目标与武器结果)、[模块边界](module-boundaries.md#机会规则与动作结果) |
| 调整武器射程访问或混合武器站位 | [决策基底](#决策基底)、[暴露与导航意图](#暴露与导航意图) | [截止目标访问机会](model-calibration.md#截止目标访问机会)、[规划包的子目录边界](module-boundaries.md#规划包的子目录边界) |
| 调整生命补充、承伤或死亡风险定价 | [动态效用](#动态效用) | [生存风险定价](model-calibration.md#生存风险定价)、[生存与碰撞](model-calibration.md#生存与碰撞) |
| 调整武器输出估计 | [武器结果预测](#武器结果预测) | [局部武器结果与目标完成](model-calibration.md#局部武器结果与目标完成)、[武器结果预测的性能边界](model-calibration.md#武器结果预测的性能边界) |
| 调整性能预算或排查掉帧 | [计算预算与搜索工作分配](#计算预算与搜索工作分配) | [性能与计算预算](model-calibration.md#性能与计算预算) |
| 复盘行为或校准模型参数 | [结果与诊断](#结果与诊断) | [战斗采样与模型校准](model-calibration.md) |
| 调整文件归属或依赖方向 | [模块边界与责任](module-boundaries.md) | [玩家权限边界](fair-play.md) |

## 运行链路

```text
RunData 扩展
└── 整局采样 ID：生成、存档与恢复

主场景扩展
├── ObservationService
│   ├── 当前观察：PlayerStateObserver、VisibleWorldObserver
│   ├── 版本知识：bot/knowledge
│   └── 局内记忆：ObservedWorldMemory
├── AutopilotController（仅在控制开启时存在）
│   ├── PhysicsFrameBudgetMonitor
│   ├── PlanningWorker（战术线程）
│   │   └── TacticalMovementPlanner：动作生成、结果预测、效用评价与选择
│   ├── PlanningWorker（战略线程）
│   │   └── StrategicNavigationPlanner：长时域价值场与导航意图
│   └── AutopilotMovementBehavior
└── BattleSampleRecorder（仅在采样开启时存在）
    └── BattleSampleWriter
```

### 生命周期与控制边界

`RunData` 扩展拥有整局采样 ID：新对局重置时生成，保存和恢复对局状态时一并存取。主场景扩展作为组合根，
按设置管理观察服务、控制器和样本记录器的波次生命周期；控制或采样至少一项开启时才创建观察服务。

启用 Autopilot 后，控制器定期读取观察并请求运动计划，只把战术计划中的首个移动方向交给
`MovementBehavior`；这是系统唯一的控制边界。战术重规划按三个物理 tick、战略指导按十二个物理 tick
的绝对节拍推进。各自的后台计算占用本级周期而不会被额外加在周期之后；若计算跨过下一个起点，控制器
在该工作线程空闲后直接提交最新快照，不追赶过时的积压请求。波次清场时，主场景扩展先停止控制器并
恢复原移动行为，再关闭样本记录器，最后由原版释放战斗节点。游戏暂停时，观察、规划和采样一并停止。
速度、碰撞、击退、动画、瞄准、攻击触发和移动机制均由原版 `Player`、`Unit` 与武器系统负责。

### 观察与规划交接

观察层每个物理帧更新可见世界、运动估计、里程计和记忆，但只在读取时物化观察快照；这样持续感知仍为
原物理频率，而随局内记忆增长的轨迹与实体副本只按重规划频率构造一次。公共读取保留防御性深拷贝；
`ObservationService` 另行截取与观察状态隔离的规划值快照，其中只含已经脱离场景节点的嵌套值，不携带
规划不消费的机制证据副本。控制器把互相隔离的快照、当前仍生效的移动输入、请求创建时刻和帧预算上下文
分别提交给战术与战略 `PlanningWorker` 实例；每个工作线程内部创建并独占每位玩家的规划器，在同一线程
配置、执行和释放其可变模型状态，然后只返回值结果。控制器在主线程保存最新完整战略指导，并只在来源帧
不超过两个战略周期时将其深拷贝给战术线程；过期或尚未生成指导时，战术规划使用零导航价值场，仍完整
执行局部动作评价。每个 `PlanningWorker` 独占 Godot 3 的长期线程、信号量、互斥交接和退出协议，不读取
或回调活动场景树。工作线程无法启动时，控制器不安装动作适配器；运行中拒绝请求时，控制器停止
Autopilot 并恢复原移动行为。两种情况都不会在物理线程降级执行规划。

后台计算期间旧移动输入仍然生效。规划器把已发生的排队时间、上一轮自身规划耗时 EMA 和一个物理帧
轮询上界合成预计动作生效延迟，再由 `ActuationStateProjector` 把脱离场景节点的观察统一推进到预计
生效时刻；玩家、敌人、解析弹道、可见与记忆实体、地图边界以及公开倒计时使用同一因果前缀。候选动作
从这个投影状态开始比较，原始采样观察不被修改。该投影只修正异步交接的时空基准，不选择避险方向或
改变任何机会价值。

### 采样与性能反馈

`BattleSampleRecorder` 独立于控制器存在。human 分段由它按固定间隔读取公共观察中的实际移动输入；bot
分段由控制器把已经取得的观察和只读规划账本提交给它。运行中若规划工作线程拒绝新请求，记录器会先关闭
bot 分段，再转入 human 分段。记录器负责采样准入、控制来源和波次分段生命周期；
`BattleSampleWriter` 只负责在独占写入线程中压缩观察、编码 JSON Lines、刷新和落盘。分段元数据和波次
上下文各记录一次，每位玩家的固定上下文也只在其首次样本前记录。写入失败只会停止当前分段接受记录，
不改变移动控制；正常关闭会等待队列排空并写入分段结尾。性能验证方法见
[性能与计算预算](model-calibration.md#性能与计算预算)。

`PhysicsFrameBudgetMonitor` 读取 Godot 报告窗口内的物理回调峰值，在同一 `idle frame` 内只推进一次
平滑估计。引擎计时止于该边界；动作决策和计算预算准入分别归属 `TacticalMovementPlanner` 与
`PlanningComputeBudgetPolicy`。

### 依赖方向

代码依赖保持单向：主场景扩展依赖 `control`、`sampling` 与 `observation`；`control` 依赖 `observation`
与 `planning`，`sampling` 依赖 `observation`，`observation` 依赖 `knowledge`。规划层只接收已经移除场景
节点的观察字典；`bot/observation/observation_service.gd` 是观察的公共读取入口。完整入口与跨包契约见
[模块边界与责任](module-boundaries.md#运行时边界与依赖)。

## 观察契约

### 公共入口与顶层结构

控制或采样至少一项开启且玩家生成后，可以从主场景读取某位玩家的最新观察：

```gdscript
var observation: Dictionary = main.autopilot_observation_service.get_observation(player_index)
```

观察由九个顶层部分组成：

```text
physics_frame
wave_state
player_state
├── dead
├── health
├── progression
├── resources
├── inventory
├── effective_stats
├── stat_opportunity_profiles
├── runtime_stats
├── collision_radius
├── movement
├── pickup
├── weapons
└── effect_rules
party_state
localization
enemy_tracks
remembered_entities
visibility
visible_world
```

`visible_world` 只包含当前可见的树木、友方角色、构筑物、材料、消耗品、敌方投射物和生成警告。
`spawn_warnings` 中每条记录公开以下字段：

| 字段 | 含义 |
| --- | --- |
| `kind` | 固定为 `spawn_warning` |
| `relative_position` | 警告中心相对当前玩家的位置 |
| `disposition` | 从警告颜色可辨认的 `hostile`、`neutral` 或 `allied` |
| `interaction_radius` | 警告区自身的稳定碰撞半径；规划层另行叠加玩家碰撞半径 |
| `resolution_window` | 警告结束前的时间；结构沿用通用时间窗契约，当前两端相等且 `is_exact` 为真 |
| `player_overlap_defers_spawn` | 玩家在警告结束时重叠该区域，是否会使敌人换位并重新开始警告 |

观察层直接读取驱动可见加速闪烁的当前倒计时，并换算成精确 `resolution_window`。敌对警告因玩家重叠而
换位并重新开始时，原版倒计时随可见相位一起重置，观察层无需维护并行的首次观测计时器。该字段的权限
依据见 [玩家权限边界](fair-play.md#判定原则)。

可见材料的 `material_quantity` 直接取原版拾取和波末转换所消费的当前 `Gold.value`。该结算值在材料形成后
已经确定，不包含未来随机结果；它随实体一起进入记忆，使已见材料离开视野后仍保留相同数量。
可见消耗品的 `pickup_profile` 将原版效果资源编译成稳定画像：`base_recovery` 与
`base_health_damage` 分别表示基础恢复和基础直接伤害，`traits` 保留可见外观能够区分且可供规则条件使用的
语义特征。当前目标版本的消耗品不会同时包含基础恢复和基础直接伤害；玩家对生命效果的修正另由
`effect_rules` 公开，不写回实体画像。
`wave_state` 包含当前波次、最终波次、无尽模式与潮汐波标记、剩余秒数和波次时长。
`player_state.runtime_stats.minimum_invincibility_seconds` 由原版 `Player.get_iframes(0)` 在观察时给出，因而
包含当前无尽波次缩放；规划层不复制这条版本规则。
`player_state.stat_opportunity_profiles` 按规范属性名提供未来事件概率曲线。每条画像包含 `curve`、
`scale` 和 `chance_limits`；这些目标版本参数由属性知识模块适配，规划层据此计算属性变化的边际机会。
当前或最近观察到的敌对单位统一由 `enemy_tracks` 表示：可见时更新测量值，离开视野后根据最后一次速度与衰减加速度
估计位置，并附带距上次出现的时间、置信度和不确定范围。`visual_radius` 通常从精灵尺寸估算，只用于
可见性、邻近压力和命中路径交会等外观尺度职责。伤害碰撞改用 `contact_radius`：观察层将标准圆形或矩形
`CollisionShape2D` 转换成以实体记录位置为中心、包围实际命中形状的半径。`visual_radius` 不得替代该
碰撞契约；未通过标准碰撞节点公开受支持形状的扩展内容不属于当前支持范围，必须先扩展机制入口。

### 实体记忆与存在证据

`remembered_entities` 永久保存本局中已经合法看见过的材料、消耗品、树和构筑物。实体持续可见时，
同一条记录会刷新位置、运动状态和最后可见时间；离开视野后保留最后观测。记录证明观察确实发生过；
`existence_confidence` 只表达该记录对应的实体现在仍存在的证据强度，不表达价值、位置精度或时间新鲜度。
时间本身不构成消失证据，置信度下降也不会删除历史记录。

拾取物的存在证据按观察状态区分：

| 状态 | 存在语义 |
| --- | --- |
| 当前可见 | 实体已经直接出现，`existence_confidence = 1` |
| 单人失视消耗品 | 没有其他拾取者；玩家进入吸附范围不改变存在性，置信度保持 `1` |
| 多人失视记忆 | 只有已经合法定位的存活队友可能在视野外拾取；按其最后位置、速度、移动能力与拾取几何估计记录仍有效的概率 |
| 收集圈覆盖后仍未再次观察 | 玩家或当前可见队友的实际收集圈提供确定缺席证据，记录从规划视图移除 |

吸附只让原版掉落物开始向玩家移动，并不回收实体。掉落物还可能处于推出或启用碰撞的过程，因此本地
玩家进入吸附范围不能替代收集证据；没有存活队友时，失视材料和消耗品不会自行降低存在置信度。
材料还可能被具有材料吸收机制的敌人取得。当前竞速模型只评价同时可见的材料与吸收者；失视后的实际
结算既不允许从场景树读取，也尚未形成存在概率，因此记忆层保留最后观测。这是材料竞速的模型边界，
不能解释为对隐藏材料现状的直接观察。
无雾波次中，合法视口重新覆盖静止树木的最后观测位置且没有再次看到它时，可以确认该树已消失；敌人
消失后则先以最后速度、稳定移动速度及冲撞
速度的最大值形成可达圆，只有整个可达圆仍被合法视口覆盖时才确认其不存在。这样已经击杀的近距离敌人
不会在固定记忆期内继续参与完成价值竞争，靠近视口边缘后可能离开的敌人仍会保留。雾区没有等价的矩形
负面证据。构筑物与未重访树木只保留观测事实，不作任意时间衰减。

静止实体的最后观测位置不会随时间漂移；只有已观察到运动的实体才会扩大位置不确定范围。
`party_state` 公开队友总数、存活队友数和存活队友索引，但不公开不可见队友的位置或隐藏行为。原版
实体池可能复用节点来生成新的材料或消耗品，记忆层会根据位置可达性分配新的 `memory_record_id`，
避免覆盖旧记录。

因此，全图决策使用“当前可见信息 + 永久观测记录 + 当前存在证据”。从未看见的实体不会进入记忆；
已见实体是否确定存在取决于上述证据，不由距离或失视时长单独决定。

### 运动状态

观察器优先保留原版为可见实体维护的当帧物理速度；原版没有提供权威速度时，才根据连续可见位置计算
后备速度。加速度统一由连续速度样本估计，并经过平滑和限幅。规划器用快速衰减的加速度模型短时外推
弯曲或变速趋势；首次看到、观察中断或样本不足时自动退化为当前速度或匀速预测。该过程不读取移动目标、
攻击目标、随机状态或未来行为。

当前可见的敌方投射物通过 `visible_world.enemy_projectiles` 公开以下运动字段：

| 字段 | 含义 |
| --- | --- |
| `relative_position` | 当前实际命中形状中心相对玩家的位置；动画位移命中形状时不退回弹体锚点 |
| `velocity` | 当前基础世界坐标速度；解析运动模型的速度偏移另行叠加 |
| `acceleration` | 由连续可见样本估计并经过平滑、限幅的世界坐标加速度 |
| `motion_confidence` | 加速度趋势的可信权重，范围为 `0.0–1.0` |
| `visual_radius` | 从可见外观估计的半径 |
| `contact_radius` | 完整世界变换后包围实际命中形状的有限半径；敌方投射物观察契约的必需字段 |
| `motion_model` | 已形成弹道的解析运动模型；正弦弹公开速度振幅、角频率和当前相位，普通弹为线性模型 |
| `contact_damage` | 当前可见投射物一次命中的原始伤害，用于生命风险预算 |

敌人通过 `enemy_tracks` 公开当前估计位置和以下运动状态：

| 字段 | 含义 |
| --- | --- |
| `relative_position` | 当前可见位置，或离开视野后的估计位置 |
| `last_observed_velocity` | 最后一次可见时测得的速度 |
| `last_observed_acceleration` | 最后一次可见时估计的加速度 |
| `estimated_velocity` | 将已观察加速度衰减到当前时刻后得到的速度估计 |
| `estimated_acceleration` | 衰减到当前时刻的加速度估计 |
| `motion_confidence` | 用于加速度外推的可信权重；离开视野后持续降低 |
| `seconds_since_seen` | 距最后一次可见的秒数 |
| `uncertainty_radius` | 离开视野后随时间和速度扩大的位置不确定半径 |
| `recency_confidence` | 最后视觉位置仍可用于空间决策的置信度；不会被血条状态刷新 |
| `existence_confidence` | 当前仍存活的置信度；持续血条目标在失视期间保持为 `1` |
| `persistent_health_observation_active` | 当前是否由原版持续血条观察提供存活与生命状态 |

速度和加速度分别使用世界坐标单位/秒与世界坐标单位/秒²。`motion_confidence` 只调节没有解析机制时的
加速度趋势；速度仍作为最低阶运动估计。观察层优先保留原版为可见实体维护的当帧物理速度；仅当原版
没有提供权威速度时，才以连续可见位置差分补足。正弦横摆一类已形成的稳定曲线弹道由当前相位、速度
振幅和角频率唯一确定，规划器直接积分原版速度方程；发射方向、散布或速度的随机结果在弹体出现后已经
体现为可见基础速度。尚未发射且仍含随机分支的未来弹道只保留机制区间，不读取随机结果。规划层消费
`estimated_velocity` 和 `estimated_acceleration`，而
`last_observed_*` 字段保留最后一次可见时的测量，供诊断和记忆解释。

敌人稳定机制还可形成 `behavior_profile.target_position_response` 与
`behavior_profile.charge_attack`。前者描述移动行为是否响应目标位置、稳定移动速度、偏好距离，以及进入
偏好距离内后停住还是向外恢复距离。规划器沿候选玩家路径分步积分该响应，近似原版逐物理帧重新归一化
方向的语义；没有稳定响应机制或敌人已经进入锁向高速运动时，改用观测运动外推。后者描述冲撞的触发
距离、速度、持续时间、可达距离、稳定冷却区间和目标分布。
`next_charge_attack_window` 与齐射时间窗采用同一公开规则：确定冷却给出精确时间，尚未完成的随机冷却
只给出稳定区间；冷却归零并已形成可见攻击准备时公开立即就绪。对尚未揭示随机瞄准结果的冲撞，规划器
保留目标区域的概率分布，以固定五点矩匹配求积近似各候选的交会风险，并在每个求积点解析计算 TTC；
只有实际到达相应时域的概率质量才进入导航、动作预测或提交期风险。冲撞已经形成可见高速运动后改由
观测路径拥有，避免重复计值。
横向脱离、沿线停留或径直后退的差异由相对运动和到达时间产生，不编码闪避动作方向。
对齐射时刻精确、朝向目标、弹速和射程稳定的攻击，环境模型会在弹体生成前按稳定投放方程形成
候选相关的未来射击走廊；候选离开走廊后压力自然下降。方向仍含随机分支、时刻只有区间或投放模式不
朝向目标时，只保留各向同性的远程火力压力，直到可见弹体公开已经实现的轨迹。
这些版本知识无需以本局受击为前提。

可见树木的 `destructible_profile` 公开稳定的最大生命、破坏命中上限和 `death_rewards`；死亡奖励画像包含
材料数量、基础消耗品掉率、消耗品成为箱子的条件概率及必掉条件。其中 `destruction.hit_limit` 表示命中
上限而不是预计完成需求。`destruction_state` 公开可见时原版剩余生命、已受击数与距上限的剩余命中数，
离开视野后作为最后一次合法测量随实体记忆保留。稳定机制、可变状态和玩家状态彼此分离：
幸运、当前生命、一击树或保树效果只在规划层换算成当轮边际价值。

玩家自身的 `movement.knockback_velocity` 直接读取原版 `Unit.get_next_velocity()` 使用的击退分量，
且是运动学预测中外部扰动的唯一来源。`RigidBody2D.linear_velocity` 在波次生成时包含单帧位置重置伪影，
且没有规划消费者，因此不进入观察或采样契约。

### 友方角色与构筑物

可见构筑物由 `visible_world.structures` 单独表示，并带有与投射物相同的当前运动字段；这使游走机器人
一类移动作用区也只能按已观察运动外推。`influence.combat_support` 描述炮塔射程、地雷一次性爆炸区或
减速场，`influence.healing_support` 描述治疗覆盖；战斗支援分别公开玩家启用半径、玩家/敌人触发半径，
避免把持续启用条件与一次性触发事件混为一谈。公共画像还公开作用半径、相对强度、是否一次性、
同时目标容量和是否依赖恢复机会。花园已经生成的果实通过可见消耗品参与规划，不把未知冷却或未来产出
提前计分。

其他玩家、宠物和转化敌人统一由 `visible_world.allied_agents` 表示，公开 `kind`、归属玩家、与当前玩家的关系、
可见运动字段和作用画像。规划器消费压力减免区、治疗区、投射物拦截区，以及其他玩家实体碰撞压力；
不接收“战斗支援”“治疗者”“拦截者”等角色标签。没有完整且合法的协作状态契约时，也不产生额外的
靠近收益。

敌对与转化状态沿用原版自动武器的目标域，具体版本入口见
[原版敌人、树木与投射物机制参考](vanilla-enemy-mechanics.md#敌人与投射物)。敌人转化后，观察记忆凭同源
节点的阵营变化立即终止旧敌对轨迹，不等待失视超时或无雾视口的缺席证据。该单位随后只以可见友方实体
参与运动和身体几何；在形成通用、可验证的友方攻击画像前，不预支其伤害支援。

### 敌人行为画像

场景节点只在观察层内部充当连续性标记，不会进入公共观察。敌人的 `behavior_profile` 由
`EnemyMechanicCompiler`、`EnemyAttackTimingObserver` 和 `EnemyBehaviorProfiler` 依次构建：机制编译器从可见
敌人的稳定配置编译攻击、位置响应、耐久和战场后果，其中位置相关机制委托给
`EnemyMotionMechanicCompiler`；攻击时序观察器形成当前的下一轮齐射与冲撞时间窗；画像器再融合稳定机制、
当前时序和未知内容的本局发射归因。稳定内容 ID 只作为编译器的内部缓存键；公共观察不包含内容 ID、
场景节点。死亡奖励画像在每次可见测量时另行适配，以纳入当前数量期望和单位效果，不进入按内容
缓存的稳定机制。可见敌人的当前生命作为 `last_measurement.health` 中的战斗期测量公开；普通敌人离开视野后
保留最后合法测量。原版持续血条可感知的 Boss/Elite 则通过独立持续血条观察继续刷新当前生命和存活，
但该观察不刷新视觉位置。稳定最大生命仍由机制画像提供，两者不得混入同一缓存。
观察器从原版完整敌人集合中取敌对域，因此仍为敌对的普通敌人、精英与 Boss 进入同一轨迹协议；模型不
根据存储容器或内容身份另设策略。转化单位按上一节的友方契约处理。

因此，具有 `ShootingAttackBehavior` 的可见敌人会立即贡献已确认的远程火力压力，无需等到它在本局
首次开火。敌人节点下常驻的敌方投射物也会编译为 `attached_orbit` 压力并进入可见弹道观察，覆盖
腐化树和 `predator` 一类不经过主敌方投射物容器的危险。对没有标准攻击配置的扩展内容，附近出现并
向外运动的可见敌方投射物仍可形成基于观察的发射归因；它与稳定机制编译是两种证据来源，不用于填补
缺失的碰撞几何。该证据随敌人轨迹保留，并在轨迹过期时消失。

`enemy_tracks[*].behavior_profile` 公开规划所需的稳定机制与观察结论。运动预测可以依据已确认机制选择相应
的运动方程，但该差异只属于状态估计：下游风险、机会和导航统一消费预计时空状态，不据此形成敌人类别、
固定目标或行为优先级：

| 字段 | 含义 |
| --- | --- |
| `projectile_attack` | 投射物攻击类别、知识来源、置信度、射程、投射物参数、射击间隔、随机边界、投放模式和火力强度 |
| `next_volley_window` | 下一轮齐射时间窗；确定性冷却给出精确时间，随机冷却只给出稳定区间 |
| `target_position_response` | 敌人对目标位置变化的连续移动响应、偏好距离、距离内响应方式和稳定速度 |
| `charge_attack` | 冲撞的触发范围、速度、持续时间、最大可达距离、冷却区间，以及玩家位置与玩家周边随机区域的目标权重和范围 |
| `next_charge_attack_window` | 当前下一次冲撞时间窗；确定性与随机冷却的公开规则同齐射时间窗 |
| `durability.maximum_health` | 稳定机制提供的最大生命基准 |
| `contact_radius` | 敌人标准圆形接触伤害形状的实测半径；稳定机制画像的必需字段 |
| `contact_damage` | 当前可见敌人的接触伤害 |
| `death_rewards.material_quantity` | 目标版本 `get_stats_value()` 返回的当前确定材料数量，再乘以当前单位机制决定的材料倍率；不含材料掉落概率 |
| `death_rewards.material_drop_guaranteed` | 是否绕过敌人的波次材料掉落概率；树木和原版强制材料掉落目标为真 |
| `death_rewards.base_consumable_drop_chance` | 原版配置提供的任意消耗品基础概率，尚未结合当前幸运等状态 |
| `death_rewards.item_box_conditional_chance` | 已产生消耗品时成为箱子的基础条件概率，尚未结合当前幸运等状态 |
| `death_rewards.consumable_drop_guaranteed` | 是否绕过任意消耗品的概率判定 |
| `death_rewards.guaranteed_death_products` | 死亡必定产生的可见产物类型及原版最大生成位移 |
| `death_rewards.stat_changes` | 死亡结算确定施加的属性、运算与数值；规划层按当前状态计算边际价值 |
| `battlefield_effects.hostile_population_per_second` | 敌人存活期间按稳定攻击机制生成敌人的速率 |
| `battlefield_effects.maximum_lifetime_hostile_population` | 有限次生成机制在完整生命周期内的稳定生成上限；无限机制为 `null`，不表示当前剩余次数 |
| `battlefield_effects.amplification_*` | 每秒强化激活数，以及每次激活施加的生命、伤害和速度比例 |
| `battlefield_effects.enemy_healing_base`、`enemy_healing_per_wave` | 敌人进入治疗触发区时的基础治疗量与逐波增量 |
| `battlefield_effects.player_healing_base`、`player_healing_per_wave` | 玩家进入同一治疗触发区时的基础治疗量与逐波增量 |
| `battlefield_effects.enemy_healing_radius` | 敌我治疗共用的实际触发区半径 |
| `material_assimilation` | 材料吸附与拾取半径、进化材料阈值及最高耐久倍率；存活负担解除价值由当前材料竞速几何派生，不包含敌人类别优先级 |
| `removal_effects.visible_projectile_damage` | 与来源一同删除的当前可见投射物原始伤害总量 |
| `removal_effects.spawned_hostile_population` | 击杀该敌人会立即生成的稳定敌人数；它作为死亡后果进入独立成本通道 |

`enemy_tracks[*].last_measurement.health` 公开当前合法感知的 `current`、`maximum` 和 `ratio`：普通目标来自
最后一次视觉测量，持续血条目标则在视野外继续更新。敌人完成需求和局部武器伤害上限使用该剩余生命；
完成价值按当前单次伤害对应的 `ceil(剩余生命 / 单次伤害)` 离散击打次数兑现，溢出伤害不产生额外价值。
字段缺失时才退回稳定最大生命先验。这样残血目标自然具有较低的完成成本，同时不会用持续血条观察凭空
制造位置精度。

敌人机制画像还可声明 `death_rewards.guaranteed_death_products`。该契约同时包含原版生成产物相对死亡
位置的最大位移。观察层只处理首次看见、类型一致且位于失视目标机制支持域内的产物；仅当这个产物在已
追踪目标中只有一个合法来源候选时，才把它作为足以结清轨迹的死亡证据。多个目标共享同一支持域时保持
未知，不按最近距离强行认领；每条目标轨迹最多消费一个产物。静止树木则长期保留已知位置和最后破坏
状态，直到重新覆盖该位置确认其不存在；确认不存在的实体和已结清敌人不会进入规划快照。

`projectile_attack.knowledge_source` 区分 `stable_mechanics` 与 `observed_emission`。前者是目标版本的稳定
机制知识；后者是已经观察到发射行为后形成的局内证据，只补充远程压力画像，不补充稳定机制或碰撞字段。

### 地图定位

`localization` 从移动里程计开始。看到左侧或上侧地图边界后，才能逐轴确定地图坐标；其他已见边界用于
补充当前边缘距离和已知地图尺寸。观察记忆还以四分之一短边视口为网格尺度记录合法视野覆盖。该网格只
描述“何处曾经可见、距今多久”，不记录格内隐藏内容，也不读取尚未观察到的完整地图位置。

| 字段 | 含义 |
| --- | --- |
| `odometry_position` | 从本局初始位置累计的玩家位移；不要求地图边界已经定位 |
| `map_x`、`map_y`、`map_position` | 看到对应左侧或上侧边界后才能确定的地图坐标；未知轴为 `null` |
| `map_bounds` | 四条边是否已见、当前距已见边界的距离，以及可由两侧边界确定的地图尺寸 |
| `observation_grid_cell_size` | 由首次有效视口短边的四分之一派生的正方形观察网格边长 |
| `observation_cells[*].grid_x`、`grid_y` | 已被合法视口覆盖过的里程计网格坐标 |
| `observation_cells[*].seconds_since_observed` | 该网格距最近一次合法视口覆盖的秒数 |

覆盖计算使用 `visibility.viewport_size` 与 `viewport_offset_from_player`，因此视口不以玩家为中心时仍沿用
实际可见矩形。未出现于 `observation_cells` 的网格表示尚无合法覆盖记录，不表示其中安全或没有实体。

## 机制知识

机制知识按信息所有者适配或编译。外部实体只有通过可见性判定后，才能调用对应知识模块；玩家自身的
透明效果数据则直接进入玩家效果适配边界。

### 敌人机制

`EnemyMechanicCompiler` 是敌人可见后稳定机制的聚合入口。它直接编译投射物攻击、耐久、接触伤害与
接触形状、奖励和战场效果，并委托 `EnemyMotionMechanicCompiler` 编译目标位置响应与冲撞。Boss 各阶段
攻击和普通敌人的附加攻击统一从 `_all_attack_behaviors` 聚合。稳定配置可以包含攻击区间和随机边界，
但不包含本轮已经抽取的结果。

当前攻击时序归观察层的 `EnemyAttackTimingObserver` 所有，并分别形成 `next_volley_window` 与
`next_charge_attack_window`。确定冷却公开精确时间，未完成的随机冷却只公开稳定区间，已经可见的攻击
准备公开立即就绪；当前目标、当前生命和随机状态不进入稳定机制画像，其中当前生命由观察层作为可见
测量单独公开。公共字段及其含义由前文的
[敌人行为画像](#敌人行为画像)定义。

目标版本的敌人清单、树木完成机制、投射物形态、实现映射和升级复核步骤见
[原版敌人、树木与投射物机制参考](vanilla-enemy-mechanics.md)。目标游戏版本的覆盖结论独立于架构契约维护。

### 玩家效果规则

原版先把玩家携带内容聚合为 `effects`。`bot/knowledge/player_effects` 是这套目标版本存储格式的适配边界，
只把已理解的字段翻译为统一规则。每条规则由事件、作用于事件载荷的条件和状态变换组成；条件描述可组合
事实，状态变换使用 `target`、`operation` 和参数表达。规则的支持范围由这些正交轴定义，不以角色、
物品、武器或扩展内容的目录衡量。

`PlayerRuleProjector` 沿上述轴产生恢复、生存与动作状态的规则投影，结果预测器则把
同一规则应用到候选路径。两者都不能读取原版字段名或内容 ID。可见消耗品只公开基础恢复、基础直接伤害
和语义特征；实体特征由通用条件匹配，不以“能否触发某项机制”的布尔字段扩张观察契约。玩家的通用
消耗品生命修正以 `consumable_health_effect` 后果公开：恢复型消耗品将其计入恢复，伤害型消耗品将其计入
伤害，正修正不会改变生命效果的方向。

死亡奖励规则同样只公开事件、条件与后果。例如，按击杀距离缩放敌人材料的原版资源会被编译为
`enemy_death` 事件上的距离曲线；曲线参数随观察快照脱离场景节点，规划层不会保留原版资源或角色身份。
规则只描述奖励如何随事件状态变化，不要求玩家持续移动，也不指定接近或远离方向。

`StatOpportunityProfileAdapter` 把属性影响未来事件概率的目标版本参数适配为
`stat_opportunity_profiles`。曲线参数属于版本知识；材料等价基准和剩余机会时域属于规划层估值。

### 武器攻击模型

武器由 `bot/knowledge/weapons/weapon_mechanic_compiler.gd` 读取目标版本的运行时状态和效果资源，编译出
不含节点、内容 ID 或机制名称的 `attack_model`。参与决策的最小契约是三个正交状态轴和一套状态转移语言：

| 轴 | 回答的问题 |
| --- | --- |
| `timing` | 当前可感知冷却、攻击进行状态、已开始近战的预计接触时间窗与长期交付速率，包括长装填折入后的期望周期和新攻击的移动许可 |
| `delivery` | 能把命中投送给谁，以锁定距离、最大飞行距离、飞行速度、路径数、角覆盖、走廊宽度、方向误差、路径容量、伤害保留、重定向选择与容量表达 |
| `impact` | 一次基础命中产生多少伤害、暴击和生命偷取，以及受哪些玩家属性缩放 |
| `rules` | 状态转移语言：已定义事件如何变换前三轴或产生结果，由事件、条件和状态变换组成 |

暴击增加贯穿、持续伤害、邻近目标伤害、重新选目标伤害和按目标耐久补足伤害都只是
`rules` 中已有轴的组合，不会成为新的决策维度。原版 `Effect` 子类只允许在编译器中决定如何翻译；规划层
只解释事件、条件、目标、操作、数值来源，以及目标与触发位置间的空间和容量约束。

玩家与武器的 `rules` 使用同一 `consequence` 契约：`probability` 表达发生概率，`target` 与 `operation`
表达状态转移，`amount` 是常量及已建模状态量的线性组合，`delivery` 只含目标选择、是否复用或排除事件
目标、是否以事件实体为锚点、半径、飞行速度、最大飞行距离和每事件容量。
`minimum_targeting_distance` 与 `maximum_targeting_distance` 以目标中心距离界定自动锁定范围；目标版本的
`Weapon.should_shoot()` 在 `current_stats.max_range` 外另有固定 `50` 像素准入量，由武器机制编译器统一写入
`maximum_targeting_distance`。目标外观半径不得扩大锁定区，只能参与命中路径的几何交会；接触半径仍
专属于伤害碰撞。
`maximum_travel_distance` 则界定路径或后续事件的传播距离。版本知识模块可以识别原版机制来编译这些量，
任何规划器都不能据此恢复或分派机制类别。

武器自身按若干次归属击杀获得属性时，编译器把每次属性增长所需的归属击杀数和属性增量写入
`enemy_kill` 规则；规则后果以 `credited_kills_per_stat_gain` 保留触发间隔，并以
`stat_upgrade_equivalents_per_credited_kill` 提供每次归属击杀的线性进度价值。
结果预测不直接猜测“最后一击”：所有武器先把命中与伤害工作附在同一 `target_id`，完成结算后再按各武器
的期望命中占比分摊目标完成量。上述线性进度价值只奖励参与目标完成的武器，但不是原版最后一击归属的
精确预测；波末未兑现进度会造成上偏，须由实测兑现率校准，也不会被写入已经获得的当前属性。

### 攻击模型的当前边界与扩展条件

公共攻击模型尚未覆盖以下三类能力。下表同时规定当前行为、重新设计的条件和责任边界；它们不是按内容
名称积累的适配待办：

| 问题 | 当前决策 | 重新打开设计的条件 | 所有者 |
| --- | --- | --- | --- |
| 非武器自主伤害源 | 当前结果预测只汇总玩家武器；不把周期弹幕或击杀派生攻击塞进物品规则特例 | 该来源能稳定改变移动位置上的期望结果，且可从合法状态估计其单位时间容量 | 将 `attack_model` 泛化为通用攻击源的 `bot/knowledge` 机制编译器与结果预测模型 |
| 目标条件伤害 | 当前生命已作为可见生命测量用于完成成本，但 `impact.rules` 尚未编译按当前生命追加伤害或依赖其他目标状态的连锁 | 条件能够由合法目标测量表达、跨内容复用，并有通用规则容量模型 | `bot/observation` 的统一目标测量与武器规则编译器；版本映射模块不得自行读取额外目标状态 |
| 命中后的运动反馈 | 当前动作内的敌人轨迹不因尚未发生的击退、减速或扩散效果而改写；下一次重规划使用新观测 | 游戏内回放能校准通用状态转移，并证明它会实质改变动作排序 | 运动观察与动作期状态转移预测器共同拥有，不能由单个效果修补轨迹 |

没有单独列为扩展点的行为已经由现有契约决定：敌人完成价值使用最后观测剩余生命形成完成份额，缺失时退回
最大生命先验；一次攻击只对其
预测时刻已存在的合法目标计分，不猜测返程前可能新入场的实体；预测窗外的周期属性、波间收益和随机升级
不提前计分，实际生效后由下一次观察和重规划自然接管。

新增能力优先组合已有的事件、条件和状态变换。只有共享模型缺少必要表达能力时才扩展正交轴，
并使现有规则能够自然复用。目标版本的原版字段映射只是局部适配代码；本文只描述规则轴、预测语义和
扩展条件；按内容核对的版本覆盖分别由敌人机制参考和玩家效果与武器机制审计维护。

### 友方与构筑物作用画像

`AllyMechanicCompiler` 与 `StructureMechanicCompiler` 把可见玩家、宠物、转化单位和构筑物适配到同一
作用画像契约；已支持的机制编译为数值化作用区。编译器可以在边界内识别原版类型，但规划层只接收
几何、强度、启用条件、触发条件、容量和恢复机会约束。
两者都不读取当前目标、冷却、生命或未知的未来产出。

## 滚动规划

规划器采用多速率分层滚动时域控制近似。动作 `u` 的效用由近端动作窗内的环境暴露、战斗与事件结果，
默认局部窗内的条件接触生命风险，以及更远的导航轨迹价值组成。近端窗之外只延续已有受支持轨迹的
几何接触扫掠，不重复计算武器、拾取、规则或完整环境通道；更远环境变化由战略价值场负责。
战术控制期由三个物理 tick 派生；战略价值场每四个战术控制期刷新。提交期是候选在新观察到来前实际执行的部分，并受
控制期、动作预测窗和本波剩余时间共同裁剪。控制器每次只执行一个提交期，然后用新观察重新求解。

### 决策流程

每次重规划依次执行以下步骤：

1. 控制器为战术与战略环分别提供规划值快照、当前仍生效的移动输入、请求创建时刻，以及由实测物理回调
   峰值形成的帧预算上下文。总后台容量按战术 `0.8`、战略 `0.2` 分账；两个规划器各自把已发生的排队时间
   与本线程近期延迟估计合成为预计结果生效延迟，
   将整个值快照推进到同一预计生效时刻。随后用保守位移上界排除在导航时域内无法影响玩家可达区域的
   投射物。完整敌人轨迹仍参与全局价值与导航；局部动作评价另以玩家位移、敌人观测速度、
   加速度、稳定追踪/冲撞速度、位置不确定范围、压力作用半径和武器锁定距离构造敌人交互域。能够在默认
   局部接触窗内威胁玩家的轨迹会保留；可见轨迹还会在能够进入自动武器锁定区时保留。其余远程轨迹只参与导航、
   敌人完成价值与波内完成预测，不进入不会受其影响的局部动作精算。计算预算政策把每位存活玩家可用的
   各自控制窗聚合帧余量与容量份额转换为最终截止和连续预算压力。战术动作生成器保留由碰撞几何派生的
   局部均匀动作；战略搜索工作分配器随压力在四至八个均匀方向间分配近似导航基线并收缩机会评价，战术
   搜索工作分配器独立收缩动作角区间细化。
   局部均匀动作、零输入和碰撞时间采样不受该预算削减，因此预算不会删除由动作几何提供的基础机动覆盖。
2. 战略线程中的导航意图规划器先比较均匀基线方向，再以机会聚合方向和优胜角区间细分扩大预算内搜索。每个实际评价
   的方向都沿完整可达轨迹在多个未来时刻采样，比较材料、恢复、拾取规则完成价值、可见未来事件、
   截止目标访问机会和环境暴露的时均差，以及沿途去重累计的地图信息。
   动态敌人与环境暴露都以同一未来时刻的零输入状态为反事实基线，只把玩家移动实际改变的部分归给动作。
   地图项使用沿候选轨迹的视口扫掠并按网格去重，再按观察陈旧度计价：未见网格具有完整再观察
   价值，已见网格随距上次观察的时间连续恢复该价值。因此未知区域产生探索，地图全部定位后也会自然
   巡游较久未观察的区域，无需预设巡逻路线。只有正增益轨迹会形成移动偏好。
   这是滚动时域控制的场采样近似：搜索层比较可达轨迹，空间机会层只估计玩家移动对目标在清理前进入锁定
   区间的可达性造成的改变；最近目标次序、额外投送路径和实际完成价值留给局部动作的武器
   结果预测。控制器仍只执行
   一个控制期并重新观察，因此不展开远期动作树，也不使用退火或随机采样替代已有的几何结构。
3. 战术线程以最新未过期的完整战略价值场作为零阶保持的指导输入；它只提供一个已经评价过的补充方向，
   并按方向插值动作窗外的行程价值。指导不可用时，该项为零，碰撞、生命、武器、拾取和规则结果仍从最新
   战术观察完整重算。导航风险使用与局部生存效用相同的环境风险单位价值。当前生命、防御、下一控制期内可达的最强一击
   和清场前可兑现的生命补充共同决定该价值。敌人耐久先按当前武器组合的平均主路径单次伤害换算为命中
   需求；树木则取按伤害耗尽剩余生命、达到命中上限或触发一击树效果所需命中数中的最小值，再进入同一个
   主路径攻击容量账本。逐目标独立估算只形成命中需求；所有目标分配的期望命中数之和不得超过
   本波剩余攻击容量。因此拥挤场景不会把同一段未来火力同时预支给每个
   普通敌人、特殊掉落敌人和树木；高奖励与低完成成本仍通过统一边际价值自然形成偏好。本波剩余时间只
   影响补充与收益能否兑现，不直接折价生命。
   动作生成器离散化本次可提交的移动输入，并补入已经通过轨迹评价胜出的导航方向。未经完整轨迹评价的
   搜索提案不会直接进入动作集。零向量是不输入移动指令的动作。
4. 动作只会执行到下一次重规划。提交期由整数物理 tick 派生；所有战术候选统一在四个控制步的近端
   动作窗内精算环境、武器、拾取和规则，再将几何接触前视延伸到默认局部窗；两者都截断至本波剩余
   时间。敌人密度或单条威胁的距离不会扩张任一时域。较长接触窗只扫描受局部交互域保留的实体，并按
   控制期分段扫掠候选玩家路径与已有受支持轨迹，不重复计算其他结果通道。局部上限和导航窗
   继续拥有压力尺度、未解析 TTC 与战略轨迹职责。角向分辨率取使相邻指令经过近端预测窗后
   的玩家碰撞圆仍然相交的最小偶数，即端点弦长不超过玩家碰撞直径；时间采样同样使相邻玩家位移不超过
   碰撞直径，并为每个控制期保留至少一个样本，确定性曲线弹的相位步长不超过 `π/2`。
5. 每个动作只在上述敌人交互域中生成一次战场、碰撞和事件基础结果，再由
   [武器结果预测](#武器结果预测) 沿该动作路径计算下一决策状态的修正，然后进入唯一一次完整评分。
   候选方向不会展开逐发攻击过程。
   已有受支持未来轨迹的实体由候选玩家路径与实体轨迹的连续相对扫掠判定交会。普通敌人使用目标位置
   响应后的轨迹，投射物使用其解析运动轨迹；扫掠一旦穿过标准碰撞边界就形成完整接触机会，穿入深度
   不充当命中概率。尚无受支持轨迹的未来实现才进入未解析碰撞模型：独立控制玩家使用当前速度延续假设
   下的 TTC，尚未发生的冲撞使用目标分布与解析到达时间。同一实体实现不得同时进入路径扫掠和未解析
   账户。稳定目标位置响应使用嵌入式二阶中点步长加倍：完整步
   与两个半步的位置差决定收紧或放宽步长，直到达到位置误差目标或统一的最小步长。该最小步长保证单次
   积分有界终止；所有候选使用相同参数，不随帧预算或可选搜索额度切换。
6. 近端位置扫掠与其后的接触前视段共同为敌人与投射物交会公开按来源区分的接触机会，每条保留预测时刻
   `time_seconds`、来源 `source_id`、兑现概率 `realization_probability`、原始伤害及来源是否在接触后消费。
   生命模型从已观察的当前无敌剩余时间开始，按原版顺序逐机会推进闪避、命中保护、护甲、实际伤害和
   随伤害变化的后续无敌时间，由同一状态分布得到预期生命损失、累计终止概率，以及有时序证据时的
   条件预计首次终止时刻。这样连续亚致命命中是否致死取决于
   它们的时序和中间状态，不依赖“一击或两击致死”策略。尚未公开逐次时刻的未解析交会仍保留峰值风险、
   累计接触和原始伤害加权证据作为无时序聚合账户；已解析路径与未解析分布的结果取较强值，不把同一事实
   放入两个所有者。聚合期望量本身不再被解释为独立命中的概率分布。局部碰撞
   生命成本对消耗后的即时生存缓冲积分，越接近缓冲耗尽，边际成本越高。
   超过当前即时生存缓冲的期望损失部分改用终止生命损失单价，仍只在生命库存账本中计入一次。默认局部
   接触窗内的碰撞损失与近端动作窗内的其他生命损失共同按即时生存缓冲计价；组合终止概率另按当前对局
   延续价值计价。该价值由持有材料、已有道具和武器的替代价格代理组成，使近期死亡风险随会清除的对局
   进度自然变贵。条件恢复仍进入公共效用，但在实际观察为当前生命前不降低同窗碰撞损失，避免
   连续规划重复借用同一份未来恢复。
   提交期终止碰撞风险为 `1` 时，候选会在下一次重规划前死于碰撞；若存在碰撞风险小于 `1` 的替代动作，
   该候选不再具备执行资格。除此之外，提交期与接触前视段属于同一组合生命账本，不再分别定价；组合
   终止风险和生命库存损失与攻击、存活负担解除、恢复、材料及信息收益统一交换。
7. 在最终截止与当轮搜索工作分配给出的额度内，规划器围绕当前具有执行资格的高分动作，在相邻角区间
   插入中点；每个新候选也执行与基线相同的完整预测和评分。预测窗内的条件生命风险与其他结果一样参与完整
   细分。可选搜索额度改变候选覆盖，不改变候选之间的结果语义。
8. 选择器先应用上述提交期执行资格，再选择统一账本总效用最高的动作。控制器提交所选移动向量，下一次
   规划重新读取环境。方向仍由碰撞、攻击、存活负担解除、恢复和可失去的对局延续价值共同决定；执行
   边界不编码敌人数量、生命阈值或逃跑方向。生命窃取与拾取恢复兑现到下一观察后，会自然改变生命库存
   价格。

### 决策基底

进入最终动作选择的量按责任而不是按数据来源划分为最小基底：环境暴露、碰撞风险、条件事件结果、
期望武器结果和尚未兑现的导航行程价值。一个事实可以被多个预测器观察，但只能由一个
评分量拥有其决策含义：已有受支持轨迹的交会由连续路径扫掠拥有，尚无受支持轨迹的未来实现由未解析风险分布
拥有，两者合并后才计分；候选路径进入收集半径的消耗品
形成 `expected_recovery`，并由 `consumed_consumable_recovery_supply` 结清离开地图的恢复储备；满血或溢出
拾取造成的恢复机会损失另由 `wasted_consumable_recovery` 诊断。已经落地的箱子会在波末自动收集，其道具
选择不因战斗中提前拾取而新增，因此导航只计算即时回血和拾取事件。树木或敌人尚未生成的箱子则属于击杀
收益，按箱子实际生成概率计价。近端动作窗内的拾取、路径规则和武器结果，以及默认局部接触窗内的碰撞
结果，都是持续采用候选输入时的条件结果，不宣称提交期外的事件已经发生；提交期碰撞子集另行保留为即将
执行的诊断。更远结果只能由环境暴露、远期碰撞诊断或导航轨迹价值表达。
`material_acquisition_value` 只拥有候选路径上的实际收集，并按
规划记忆中的确定数量和当前存在置信度结算；尚未拾取材料的空间价值由导航统一聚合。局部动作以原版收集
半径和连续相对交会判断实际拾取；导航在各轨迹采样时刻以同一收集半径比较可达性，并把路径已经穿过的
收集事件保留为吸收收益。已经处于收集圈内的
材料由局部结果结算，不再由导航重复定价；进入吸附范围但尚未进入收集圈时，仍保留继续接近的机会梯度。
所有候选都相同的被动恢复只影响规划上下文，不进入动作结果。

效用模型把评分字段唯一分配给五个决策目标：`survival`、`recovery`、`economy`、`combat` 和
`navigation`。调试构建在每个效用模型实例首次生成评分上下文时验证同一字段不能进入两个目标；
`objective_utility_breakdown` 公开目标级总账，`field_utility_breakdown` 保留字段级解释。规划空间不是先把
世界烘焙成两张标量栅格再相加，而是在候选位置与时刻查询一个多通道场。环境威胁、材料、恢复、拾取
规则完成价值、截止目标访问机会和地图信息先由各自所有者换算，再到导航轨迹聚合价值中相加。这样既能
诊断“高奖励但高威胁”，也不会让不同单位、时域或条件的量在进入模型时就失去含义。

敌人完成状态转移保留三个正交通道：奖励变化、存活负担解除和死亡后果。奖励变化等于敌人死亡奖励减去
失去的存活奖励；存活负担解除可以为负，例如完成一个会治疗玩家的敌人会失去未来治疗；死亡后果记录死亡
触发的新敌人等成本。`EngagementTargetProjector` 把敌人轨迹与树木统一投影为可交战目标。树木静止、
不响应玩家位置，没有存活负担或死亡后果，完成条件取生命归零、达到命中上限与一击树效果中最先满足的
一项；普通敌人、精英、Boss 与宝箱怪则由同一敌人机制画像提供运动、耐久、收益和负担。波内完成预测以
统一的 `target_id` 给出有界 `completion_fraction`；局部武器结果预测按原版最近目标顺序和投送路径分配
命中容量。敌人的命中、伤害、完成状态和价值始终以同一 `target_id` 关联，树木则按自身声明的生命、
命中上限和一击完成机制结算。两者分别形成 `expected_enemy_completion_equivalents` 与
`expected_tree_completion_value`，不共享结算单位。敌人完成量是由当前攻击时序容量形成的有界完成代理，
不是经过统计校准的击杀概率；具体结算见 [武器结果预测](#武器结果预测)。

`WeaponTargetingIntervalModel` 按每把武器的最小与最大锁定距离保留多个区间，并针对生命目标按主路径
伤害率、针对命中上限目标按主路径命中率归一化区间份额。`SpatialOpportunityValueModel` 再用这些区间
计算玩家移动新增或失去的截止目标访问价值。目标有受支持的自主位置响应时，模型先在零输入反事实中结算
其清理前能够自行闭合的距离，只对仍需玩家运动创造的访问缺口计价；它不解释最近目标次序、局部攻击窗口或额外群体容量，这些
职责仍归局部武器结果预测。任何一层都不包含树、精英、宝箱怪或 Boss 的预设身份优先级；追击顺序只由
当前状态下的价值、容量与可达性共同产生。

移动不会把拾取与自动攻击拆成互斥目标：每个候选移动同时比较导航路径上的材料、恢复、目标访问机会
和该路径允许的局部武器结果；动作预测窗内的交互只记为采用该候选输入时的条件结果。无稳定目标响应的
敌人沿用观测运动外推；已确认追踪响应的敌人则按候选玩家路径逐步更新方向，因此追踪经验会改变地图和
轨迹评分，却不会变成预设动作。
移动状态产生的材料周期收益归入 `economy`；护甲、闪避、武器属性和移动速度分别进入生存、攻击与
运动学预测器。

规划时间域由 `PlanningTimingModel` 唯一拥有：它从物理 tick、控制步数、玩家碰撞直径和当前速度派生
提交期、近端动作窗、默认局部接触窗和导航窗。空间机会的评分归属则由
`SpatialOpportunityValueModel` 统一处理。已确认不存在的记忆不进入机会模型；近端动作窗可直接收集的
拾取物只由动作结果结算，其余仍可能存在的可见与记忆机会由导航路径统一计值。截止目标访问机会只描述
玩家移动对清理前锁定区间可达性造成的改变，不奖励目标本来就会完成的自主接近，也不替代候选动作对局部
武器结果的预测。
动作集合包含离散方向和零输入。目标版本 `Unit.get_move_input()` 将非零移动输入归一化后乘移动速度。

### 结果与诊断

状态为 `ready` 的规划结果同时提供评分账本和只读诊断。顶层 `model` 保存统一时间契约和
本次决策实际使用的派生空间尺度；`selection_diagnostics` 保存提交期执行资格、全部与保留候选数量、
被排除的确定终止候选数，以及所选动作的提交期终止生命风险。
战术 `compute_budget.phase_duration_usec` 按观察准备、战略指导交接、基线动作完整评价与方向细分记录阶段
耗时；`strategic_navigation_guidance_diagnostics.compute_budget.phase_duration_usec.navigation` 单独记录
战略导航耗时。指导诊断还记录 `source_physics_frame` 与 `age_physics_ticks`。这些诊断只解释校准样本和
最终选择，不参与效用评分。
`context.state_factors.run_continuation_value` 记录当轮对局延续价值的持有材料、单位替代价格、道具与武器
数量及总值，供风险成本复盘。
`local_enemy_interaction_domain` 记录动作精算前后的敌人轨迹数量；`wave_completion_forecast` 记录统一目标
共享的本波主路径命中容量、根据剩余生命和命中上限形成的独立需求命中数、实际分配命中数和竞争缩放。
逐目标完成份额只留在规划上下文，不进入返回计划的遥测。
两者都不改变观察记忆，也不是行为优先级。

以下预测结果直接进入动作总分，或先由效用模型按当轮价值上下文换算。字段级账本记录换算后的有符号
贡献：

| 字段 | 含义 |
| --- | --- |
| `material_acquisition_value` | 动作预测窗内沿候选路径实际进入收集半径的可见或记忆材料期望价值，按当前存在置信度折算，并包含避免推迟至后续波次兑现的成长时机价值 |
| `consumed_consumable_recovery_supply` | 拾取消耗品时离开地图的生命补充储备，按 `replenishment_unit_value` 扣除；使恢复收益与储备消耗进入同一账本，避免重复计值 |
| `consumed_single_use_support_supply` | 候选路径由玩家或敌人触发地雷等一次性支援时消耗的储备，以支援强度和存在置信度折算，并按补给库存单价扣除 |
| `expected_committed_damage_progress_value` | 已开始的近战接触所保留的非致死伤害进度价值；只在本波仍支持完成该目标时兑现，并扣除已经兑现的完成份额，不重复计入击杀价值 |
| `expected_enemy_reward_delta_value` | 动作窗内敌人完成份额兑现的奖励净变化：敌人死亡奖励减去失去的存活奖励；未完成伤害不领取完整变化 |
| `expected_enemy_burden_relief_value` | 同一完成份额兑现的存活负担解除价值，可能因敌人提供治疗等效应为负，并随剩余波时自然缩短 |
| `expected_enemy_death_consequence_value` | 同一完成份额触发的死亡后果价值；在效用边界作为成本扣除 |
| `expected_tree_completion_value` | 树木参与自动武器目标覆盖后，按期望伤害耗尽剩余生命或达到剩余命中上限中较快的一条路径兑现收获价值；一击树效果把玩家命中需求降为一次 |
| `expected_rule_completion_value` | 拾取、治疗、受击或闪避等玩家规则事件对具体敌人造成即时伤害或状态后，按敌人剩余生命结算的完成价值；状态期间可发生的后续伤害增量受武器容量和状态时窗约束 |
| `expected_allied_completion_value` | 地雷等一次性友方作用命中当前可见爆炸范围内具体敌人后，按离散剩余击打次数产生的完成价值 |
| `expected_recovery` | 候选路径进入收集半径、生命偷取和动作相关事件规则产生的条件恢复量 |
| `expected_stat_upgrade_equivalents` | 效果规则产生的永久或临时属性变化折合成的期望一级升级份数 |
| `expected_stat_opportunity_value` | 属性变化影响未来事件机会所产生的材料等价边际价值 |
| `expected_material_gain` | 暴击击杀等战斗事件直接产生的预期材料，不含地图上已有材料的拾取 |
| `movement_damage_exposure_reduction` | 候选移动状态相对当前状态降低的护甲与闪避伤害暴露，按碰撞风险调制；负值表示暴露增加 |
| `integrated_allied_healing_support` | 动作沿途处于友方治疗或治疗增益覆盖内的累计支援暴露 |
| `integrated_environmental_exposure` | 敌人接近、敌人与地图边界共同遮蔽的机动空间、生成、远程火力和队友阻塞扣除对应减压后的沿途环境暴露；边界与敌人遮蔽同一方向时只计一次，不含碰撞，按延续时域内一单位生命损失的价值计分 |
| `field_utility_breakdown.forecast_health_inventory_loss_value` | 效用模型将默认局部接触窗内的碰撞生命消耗与近端动作窗内的其他生命消耗沿对数障碍积分，并按剩余延续时域裁剪；超过即时生存缓冲的部分改用终止生命损失单价。清场前补给不会扩大即时缓冲 |
| `field_utility_breakdown.expected_run_continuation_value_loss` | `forecast_terminal_health_risk` 与当前对局延续价值形成的负向贡献。有逐次时刻证据时，条件预计首次终止时刻越早，终止紧迫度因子越高；无时序证据时因子为 `1`。该风险成本与近端收益在同一账本中交换，不预设逃离或接敌方向。延续价值包含持有材料，并以已解锁道具池各稀有度均价中的最小正值代理已有道具与武器的单位替代价值。可存活生命损失由即时缓冲曲线单独计价，不再重复解释为死亡概率 |
| `navigation_trajectory_value_gain` | 本动作在相邻导航轨迹样本间作环形线性插值得到的单位距离聚合价值，再按输入相对零输入在动作预测窗内造成的位移兑现；不同动作使用各自方向的轨迹价值 |
| `standing_seconds`、`moving_seconds` | 对应移动状态在动作预测窗内的持续时间，只承载该状态的周期材料收益 |

以下字段记录预测证据和中间量。部分字段参与上方评分量的合成，但都不会作为独立收益或成本再次计分：

| 字段 | 含义 |
| --- | --- |
| `forecast_terminal_health_risk` | 默认局部接触窗内的碰撞终止概率与近端动作窗内其他终止分量的合成值；效用模型以此计算候选进入近期致死路径的对局延续价值损失 |
| `committed_terminal_health_risk` | 下一次重规划前各终止分量的合成概率；值为 `1` 且存在非确定终止候选时由选择器排除，不指定规避方向 |
| `forecast_terminal_collision_risk`、`committed_terminal_collision_risk` | 按接触时序推进的生命状态分布所得默认局部接触窗与提交期累积终止风险，并与尚无逐次时刻的未解析交会单次终止证据取较强值；原版受正伤即失败规则也由同一状态转移表达 |
| `forecast_terminal_health_time_seconds`、`committed_terminal_health_time_seconds` | 在发生终止的条件下，由带时刻碰撞或伤害事件状态分布得到的预计首次终止时刻；无时序聚合证据不伪造该值。前者只连续调制终止价值的紧迫度，后者保留诊断 |
| `terminal_risk_urgency_multiplier` | 终止紧迫度因子；存在 `forecast_terminal_health_time_seconds` 且终止概率大于 `0` 时才大于 `1`。它只用于合成上方对局延续价值损失，不作为第二份评分通道 |
| `forecast_terminal_consumable_risk`、`committed_terminal_consumable_risk` | 伤害型消耗品在近端动作窗与提交期的终止分量；按预计收集事件、当前无敌剩余时间、单次伤害和存在置信度形成 |
| `expected_weapon_damage` | 武器结果预测在动作预测窗内归给敌人的伤害容量；敌人完成价值另由完成等价数兑现 |
| `expected_enemy_hits` | `expected_attack_hits` 中归给敌人的有限命中容量，包括主目标、直接路径次要目标和重定向命中；每份敌人完成等价数都必须由至少一份敌人命中支持 |
| `expected_enemy_completion_equivalents` | 各敌人的离散完成代理之和；代理按同一目标的累计命中、预测单击伤害和最后观测剩余生命结算，预测窗内不能完成的目标不兑现完成价值；跨目标相加可大于 `1`，不解释为统计击杀概率 |
| `committed_expected_health_loss` | 提交期内碰撞生命影响与伤害型消耗品直接伤害的合计；保留为即将实际提交的短期诊断，不再重复解释为死亡概率或独立计分 |
| `forecast_expected_health_loss` | 默认局部接触窗内碰撞生命影响与近端动作窗内伤害型消耗品直接伤害的合计；效用模型将结果换算为 `forecast_health_inventory_loss_value` |
| `committed_consumable_health_loss`、`forecast_consumable_health_loss` | 提交期与近端动作窗内，按预计收集时刻核对当前无敌剩余时间后形成的伤害型消耗品直接生命损失；两者已经分别并入对应的总生命损失字段，不再独立计分 |
| `integrated_enemy_proximity_pressure` | 敌人距离和记忆不确定性形成的有界累计压力；尚未发生的冲撞由独立碰撞证据拥有 |
| `enemy_charge_obstacle_risk` | 已知冲撞机制按当前时序、尚未揭示的目标分布、相对运动和解析到达时间形成的未解析交会风险；该证据同时进入碰撞与生命损失账本 |
| `integrated_projectile_proximity_pressure` | 敌方投射物位置域扫掠形成的累计邻近压力 |
| `peak_projectile_contact_risk` | 位置域扫掠确认的峰值投射物接触风险 |
| `integrated_spawn_pressure` | 可见敌对生成警告外缘的累计压力；进入原版会延后生成的交互区后归零，是否值得占住该区域由未来事件机会计价 |
| `integrated_maneuver_constraint` | 可达敌人接触圆盘与已知地图边界在下一控制期内遮蔽角区间的并集；聚团、包围和边界重叠因空间拓扑自然产生不同约束，不重复表达接触结果，也不指定逃跑方向 |
| `integrated_edge_pressure` | 已知地图边界单独遮蔽的角向机动空间诊断；其决策成本已包含在 `integrated_maneuver_constraint`，不会再次计分 |
| `peak_enemy_contact_risk` | 按玩家碰撞半径与敌人稳定接触伤害半径计算的峰值接触风险 |
| `integrated_ranged_attack_pressure` | 暴露在已确认远程攻击范围内的累计压力；包含确定性目标定向齐射形成的候选相关未来射击走廊，不把它重复解释为已生成弹体接触 |
| `integrated_allied_body_pressure` | 多人模式下靠近其他玩家实体形成的累计阻塞压力 |
| `integrated_allied_pressure_relief` | 构筑物或战斗宠物对相关敌人环境压力的累计原始减压量 |
| `integrated_projectile_interception_relief` | 友方角色先于玩家截获威胁弹道后形成的累计投射物减压 |
| `integrated_hostile_exposure` | 按环境压力通道权重合成的沿途总敌对暴露 |
| `integrated_exposure_relief` | 受对应敌压上限约束的沿途总减压 |
| `peak_environmental_pressure` | 动作预测中任一采样点的峰值环境暴露 |
| `peak_path_collision_risk` | 位置域采样和扫掠得到的峰值碰撞风险 |
| `integrated_hostile_collision_risk` | 位置域敌对接触风险沿预测时间的积分，用于区分短暂交会与持续贴身 |
| `contact_opportunities`、`committed_contact_opportunities` | 近端动作窗与提交期内的按来源接触机会；每条保留 `time_seconds`、`source_id`、`realization_probability`、`raw_damage` 和 `source_consumed_on_contact`，供生命状态按时序推进，不是已观测命中 |
| `contact_forecast_seconds` | 几何接触前视的有效截止时刻，取默认局部时域与本波剩余时间的较小值 |
| `contact_lookahead_opportunities` | 近端动作窗结束后、默认局部接触窗截止前的按来源接触机会；只在完整候选结果中供生命状态推进，排序摘要会裁掉该明细以控制日志与跨线程传输成本 |
| `contact_lookahead_peak_collision_risk`、`contact_lookahead_evidence_seconds`、`contact_lookahead_raw_damage_evidence_seconds`、`contact_lookahead_maximum_raw_damage` | 接触前视段内的峰值接触、占用时间、伤害加权占用时间和最强原始伤害；只汇入碰撞生命影响，不作为独立评分字段 |
| `path_contact_evidence_seconds`、`path_raw_damage_evidence_seconds` | 位置域扫掠对各独立敌人和未被拦截投射物保留的聚合接触占用时间与伤害加权占用时间；它们是无时序诊断证据，逐次位置接触的生命解释以 `contact_opportunities` 为准 |
| `committed_peak_path_collision_risk` | 位置域峰值碰撞证据中落在本次提交期内的部分 |
| `committed_integrated_hostile_collision_risk` | 位置域累计敌对接触中落在本次提交期内的部分 |
| `committed_path_contact_evidence_seconds`、`committed_path_raw_damage_evidence_seconds` | 上述可叠加位置域接触证据中落在本次提交期内的部分 |
| `committed_hostile_unresolved_collision_risk` | 本次提交期内尚未揭示结果的敌方冲撞目标分布交会风险 |
| `forecast_hostile_unresolved_collision_risk` | 候选动作自身预测窗内尚未揭示结果的敌方冲撞目标分布交会风险 |
| `forecast_hostile_unresolved_contact_evidence_sum`、`forecast_hostile_unresolved_raw_damage_evidence_sum`、`committed_hostile_unresolved_contact_evidence_sum`、`committed_hostile_unresolved_raw_damage_evidence_sum` | 未解析交会在近端动作窗与提交期内保留的可叠加接触证据总量和原始伤害加权证据总量；用于承伤换算，不作为独立评分字段 |
| `forecast_maximum_unresolved_collision_raw_damage`、`committed_maximum_unresolved_collision_raw_damage` | 近端动作窗与提交期内未解析交会可能带来的最强单次原始伤害；只参与对应生命影响的上界换算 |
| `initial_environmental_pressure` | 候选动作起点的环境暴露 |
| `terminal_environmental_pressure` | 候选动作预测终点的环境暴露 |
| `mean_environmental_pressure_derivative` | 沿候选轨迹的环境暴露平均物质导数，即终点与起点暴露之差除以预测时长；不是固定位置随时间的变化率 |
| `unresolved_collision_risk` | 尚无受支持解析轨迹的队友未来运动或冲撞目标分布与候选动作交会的有界证据 |
| `collision_risk` | 完整局部窗内已解析路径峰值碰撞证据与未解析碰撞风险的较大值；用于远期诊断与移动状态暴露调制 |
| `ally_unresolved_collision_risk` | 与独立控制的其他玩家未来运动交会的 TTC 风险 |
| `minimum_ally_time_to_collision` | 当前候选速度下与其他玩家的最早预测交会时间；敌人与可见投射物接触由路径扫掠表达 |
| `candidate_velocity` | 结合移动输入与已观察击退衰减后的候选平均速度 |
| `maximum_armor_adjusted_hit_damage` | 预测会与候选交会的最强单次伤害经过候选护甲换算后的数值 |
| `expected_contact_resolution_count` | 提交期内进入原版伤害解析顺序的预期接触次数；当前无敌时间会阻止解析，闪避和命中保护属于解析结果而非次数扣减 |
| `forecast_expected_contact_resolution_count` | 默认局部接触窗内进入原版伤害解析顺序的预期接触次数 |
| `committed_contact_opportunity_count`、`forecast_contact_opportunity_count` | 提交期与默认局部接触窗内的原始接触机会数量；用于诊断几何输入，不直接作为评分字段 |
| `wasted_consumable_recovery` | 拾取消耗品时因满血或溢出而未转化为当前生命的恢复量 |
| `expected_attack_hits` | 武器结果预测按攻击率、覆盖和多目标容量估计的敌人与树木总命中数 |
| `expected_recovery_events` | 玩家效果规则预测得到的恢复事件数 |
| `expected_kill_weight` | 预期伤害相对敌人最后观测剩余生命形成的击杀进度；缺失测量时使用最大生命先验 |
| `expected_critical_kill_weight` | 上述进度按武器暴击率折算的暴击击杀证据 |

压力通道先在各自语义内归一化和有界叠加。局部路径与导航轨迹的环境暴露，按一单位生命损失在剩余
导航时域内的延续价值换成公共价值；耗尽即时生存缓冲的单位仍按终止价值计价。补给储备消耗和波次尺度
敌人负担按 `marginal_health_unit_value` 换算。两者共享同一材料价值基准，但后者还计入清场前可兑现补给，
而不是使用波次阶段的独立倍率。一单位材料是基础价值基准；合并材料和奖励材料按外观
可证明的单位下界计数，收集地面材料还会计入避免推迟至后续波次兑现的成长时机价值。普通属性变化按目标
版本一级升级所给的属性增量归一化，属性造成的未来机会变化另作边际定价；敌人完成状态转移把奖励变化、
直接伤害压力、剩余时间内的生产、强化和治疗后果、死亡后果，以及敌人提供的玩家治疗机会保存在可解释的
独立通道中。

### 暴露与导航意图

底层环境暴露 `P(x,t)` 是位置和时间上的连续启发式运行成本，不声称是物理压力、受伤概率或完整价值函数。
`NavigationIntentPlanner` 在有效导航时域内的可达轨迹上采样该场，用于比较大方向而非精确避弹。均匀
方向保证没有已知机会时仍能比较空间；机会聚合方向和连续角区间细分都属于
预算内搜索提案。它们只增加采样覆盖，必须经过完整轨迹评价才可能影响动作，不预选策略目标。候选轨迹
受本波有效导航时域和已知边界裁剪。

本文使用以下术语区分搜索位置、价值与可执行输入：

| 术语 | 含义 |
| --- | --- |
| 场采样 | 在未来状态 `(x, t)` 比较候选移动与同刻零输入反事实的材料、恢复、可见未来事件、目标访问和环境暴露差 |
| 轨迹聚合价值 | 沿一条可达轨迹聚合场采样，并加入沿途去重的地图信息价值 |
| 访问势能 | 尚未进入收集圈或武器射程时，由截止可达缺口和距离衰减形成的状态价值；目标能够自主闭合的射程缺口先归入零输入反事实。该值只评价可达状态，不替代动作结果中的收集、命中或完成 |
| 搜索提案 | 只指定额外评价方向；本身没有价值，也不是可执行动作 |
| 移动偏好 | 轨迹聚合价值为正的最高分已评价方向；只有它可以补入局部动作候选 |

地图信息模型沿轨迹扫掠视口，并按观察网格去重：未见网格贡献完整覆盖变化，已见网格按距上次观察的
时间相对波长连续恢复。它只计算合法视野中新增或恢复的规范化覆盖，不把未知区域视为已知安全区，也不
编码巡逻路线。效用上下文为覆盖变化提供每视口信息单价：先取基础下限、当前可见机会均值与潜在单个
消耗品恢复量的补给库存价值中的较大者，再按本波剩余比例裁剪。生命越稀缺，揭示恢复机会的价值越高；
沿途环境暴露仍按同一生命价值定价风险。这一连续交换不设置低生命模式，也不预设树木、恢复物或探索方向。

导航把候选输入展开为可达轨迹，在多个未来时刻采样尚未兑现的材料、恢复、可见未来事件、目标访问机会
和环境暴露，并沿整条轨迹对地图信息去重累计。即使有效导航时域很短，每条非空轨迹也至少包含一个波末
前采样和波末边界采样，避免只观察必然归零的清理时刻。材料和消耗品使用实际收集半径；拾取物按已观察速度投影到相应
采样时刻，进入吸附范围只改变其已观察运动，不提前兑现机会。各采样时刻只保留在剩余波时内仍能到达
收集圈的可达性；可由近端动作窗直接收集的拾取物交给动作结果独占。其余拾取物的接近进度按起点尚未
兑现的可达选项归一化，使进入收集圈兑现完整机会，而不是只取得已接近饱和的波末可达性微差。访问势能
按有效导航距离指数衰减，并在波末可达边界连续降至零。因此远处但可及时到达
的材料仍有较小的接近梯度，无法及时到达的材料既不计值，也不占用额外方向搜索预算。波末边界只保留
已经完成的收集，因此未兑现的接近进度不会作为跨波残值。

完成份额为正的敌人与树木使用同一访问势能；候选最终只取得相对零输入新增或失去的加权锁定区间访问。
空间模型把目标按候选玩家路径投影到采样时刻，再用有证据的目标位置响应结算清理前自主闭合的区间缺口，
其余缺口才进入玩家截止可达势能。目标无需玩家运动就会及时进入区间时，零输入已经持有完整访问；主动
接近不再额外获得时间偏好收益。候选把目标带到清理前无法进入区间时，仍会失去访问价值。
静止树木没有自主闭合速度：位于最大距离外时接近、位于最小距离内时远离，同样形成由截止可达性、几何
和容量自然产生的访问增量。进入区间后访问势能饱和；最近目标、投送几何、攻击容量和更早完成带来的
局部减压全部由动作窗武器结果与环境风险账本拥有，避免把完整死亡奖励同时作为导航加速收益。吸引强度
来自统一奖励、战场后果、完成工作量和可达性，而非树木或敌人类别优先级。
可见生成警告也进入同一截止可达模型：中立事件按当前信息机会价值形成在波末前接近新可攻击事件的选择；
敌对事件按已观察敌群的有符号平均负担和当前精确截止 `resolution_window.latest_seconds`，衡量及时进入
生成延后区的机会。正负担使延后生成增值，敌群整体提供净帮助时延后生成会产生负值，没有负担证据时
则为零；模型不会补入固定吸引力，也不猜测将生成的实体身份。候选与零输入基线在各时刻使用相同运动
机制，因此追击、等待或脱离仍由玩家实际创造或失去的攻击访问与同刻风险、拾取和信息机会共同决定。
导航环境暴露也比较同一未来时刻的反事实差值。每个已评价方向都保留轨迹聚合价值；动作评分在
相邻方向样本间线性插值单位距离价值，并按输入相对零输入在动作预测窗内造成的位移兑现。因此击退保持
为共同反事实，导航行程与环境暴露、承伤和武器结果使用同一个持续动作条件时域；控制器仍只提交一个
控制期并在新观察到来后重新求解。
导航意图公开方向与价值（`movement_preference`、`trajectory_value_samples`、`trajectory_value_gain`、
`selected_trajectory`）、评价次数和空间尺度（`sampling_radius`、`local_prediction_radius`、
`control_distance`），便于验证导航意图来源。其中
`trajectory_value_gain` 是胜出轨迹的聚合价值：机会与暴露按未来时刻平均，地图信息按轨迹去重累计；
它不是单个动作预测窗中的按距离兑现字段 `navigation_trajectory_value_gain`；
`baseline_field_sample_evaluation_count` 与 `extra_field_sample_evaluation_count` 分别记录基线和额外轨迹的
时空场采样总数；`extra_direction_evaluation_count` 与 `extra_direction_evaluation_limit` 记录预算化方向搜索
的实际用量与上限；`trajectory_sample_count` 记录每条轨迹的时间采样数；`trajectory_value_samples` 记录各
已评价方向的轨迹距离、聚合价值、单位距离价值和值分解；`selected_trajectory.value_breakdown` 与各方向
样本的 `value_breakdown` 都按 `material_opportunity`、`recovery_opportunity`、
`rule_event_opportunity`、`future_event_opportunity`、`target_access_opportunity`、
`map_information` 和 `environmental_exposure` 分解轨迹价值。`sampling_radius` 是地图边界裁剪前的
轨迹长度，实际长度见 `selected_trajectory.distance`。

导航轨迹的环境暴露与局部动作使用同一延续时域暴露单价。动作评价仍以更细粒度检查完整路径并执行扫掠
碰撞检测，因此导航场不会替代局部避障。候选动作还记录环境暴露沿路径的平均变化率
`[P(x(T),T)-P(x(0),0)]/T`，即 `∂P/∂t + v·∇P` 的路径平均。

各通道的抵消关系受语义约束：友方火力只能降低当前敌人接近和远程火力造成的环境暴露，不能抵消尚未
生成的警告；投射物拦截只能降低拦截时刻之后该弹原本会造成的压力。减压不能消除实体接触、角向机动
约束或队友阻塞，也不能超过当时对应的敌对压力，因此友方作用区不会产生无上限收益。治疗和治疗增益
属于独立恢复机会，不能抹去已经预测到的伤害压力。地图边界与可达敌人圆盘共同形成遮蔽角区间并集；
重叠方向只计一次，边界的单独覆盖量仅作诊断，不再形成第二项边缘成本或放大其他敌对通道。

### 动态效用

生命价值上下文在每次规划时重建。`HealthReplenishmentForecastModel` 预测清场前可兑现的生命补充：已观察
消耗品按到达时间和存在置信度折算，概率掉落按共享攻击容量约束后的目标完成份额折算，被动恢复与生命偷取
按剩余可作用时间折算。`HealthInventoryValueModel` 将这些补充与当前生命和被动生命流失合成为补给库存；
同时以玩家完整动作集合、当前击退和威胁运动在下一控制期内的联合可达域，筛出最强接触或投射物伤害
储备，并形成风险尺度和单位价值。`HealthLossValueModel` 只消费该价值上下文，将碰撞、伤害型消耗品等
任意来源的生命损失换算为同一即时缓冲成本；它不预测补充、碰撞或拾取。候选动作自己的扫掠与解析碰撞
预测仍负责区分具体方向的接触风险。
本波剩余时间界定补充、奖励与存活期威胁能否兑现。非致命生命损失的延续成本按动作窗结束后仍需生存的
导航时域比例保留；超过即时生存缓冲的部分始终按终止成本计价，不能借波末清场消失。

当前生命扣除联合可达域内的最强一击储备后形成 `immediate_survival_buffer`；该缓冲加上清场前可兑现
补充、再扣除同期被动生命流失，形成 `projected_health_inventory`。前者和剩余延续时域共同决定局部与
导航环境暴露及动作窗内非致命生命损失的成本，后者决定补给库存及波次尺度敌人负担的边际价格。整波补给
预测没有提供候选条件的近端兑现顺序，因此不能扩大当前即时生存缓冲。默认局部接触窗内的非致命碰撞成本沿
即时生存缓冲的对数障碍积分，越接近缓冲耗尽，边际成本越高。接触机会按时序推进得到的期望生命损失由生命库存损失
计价；同一状态分布得到的终止风险补足期望损失无法表达的致死尾部，包括多次亚致命接触累积成的终止结果。
原版下一波按开局生命
比例重新设置玩家生命，当前值不跨越波次边界，因此波末后没有持有当前非致命生命的残值；这会自然允许在
不会越过即时生存缓冲时以生命换取仍可兑现的收益，而不是由专用“波末冒险”开关或生命阈值触发。

`RunContinuationValueModel` 估计死亡会清除的当前对局延续价值：持有材料按一比一计价，已有道具和武器
按公共价格画像中的最低正稀有度均价估算单位替代价值。模型不读取具体道具或武器身份，也不估计尚未获得
的未来收益。`MovementUtilityModel` 以组合生命结算窗内的 `forecast_terminal_health_risk` 计算候选近期延续
价值损失；带时刻证据还按条件预计首次终止时刻相对战术控制期的接近程度，连续调制同一损失，
避免不同致死时刻在饱和概率下坍缩为同一结果。
可存活生命损失仍单独按接触窗末剩余延续时域和即时缓冲凸曲线计价。提交期风险只拥有动作
能否安全执行到下一次重规划的资格判断，不再承担终止风险计价职责。具体公式和校准证据见
[生存风险定价](model-calibration.md#生存风险定价)。

拾取恢复型消耗品会把地图生命补充转化为当前生命。`expected_recovery` 按即时生命的终止价格记录动作预测窗内的
条件恢复收益，`consumed_consumable_recovery_supply` 按补充库存价格结清离开地图的储备；两者的差恰好是
从储备转成即时生存流动性的边际价值，不会再次扣除同一份供应。树木或敌人新生成的消耗品则增加补充库存，
按库存价格计值。可兑现恢复量受缺失生命上限约束。

长时域访问势能只给玩家可以选择兑现的正净机会计价；可达性提供选择，不制造收集义务。伤害型消耗品
不进入补充库存。结果预测按预计收集时刻核对当前无敌剩余时间，把其直接伤害分别计入近端动作窗和
提交期生命损失；两者的终止分量分别进入 `forecast_terminal_health_risk` 与
`committed_terminal_health_risk`。导航将同一伤害经生命损失模型定价，致死收集还会失去对局延续价值；
候选路径实际穿过拾取物时仍结算完整伤害。同一拾取的属性、爆炸或其他规则收益可与生命成本交换；只有
净价值为正时才形成导航机会，不需要内容身份、低血阈值或固定规避方向。

`PickupCollectionProjector` 从包含当前可见实体的规划记忆中一次形成候选路径收集事件，并用存在置信度
折算恢复、伤害、储备消耗、材料和规则后果；因此暂时离开视口的合法记忆不会在局部动作边界消失。拾取
规则产生的即时伤害、持续状态及状态期间的后续伤害增量，在局部动作与导航中共享同一空间交付和敌人完成
账本：局部动作在连续收集时刻形成
`expected_rule_completion_value`，导航按候选可达性与该时刻的事件覆盖形成 `rule_event_opportunity`。
事件价值只结算候选时刻可由公开状态支持的结果，不估计一次性事件源的跨时刻保留价值。立即兑现与保留
之间的比较属于尚未实现的跨时刻状态价值边界，其证据要求见
[仍需校准的假设](model-calibration.md#仍需校准的假设)。
属性或材料效果仍由候选拾取位置上的事件预测器计算。地面箱子与果实采用相同的收集和事件规则；箱子携带
的道具选择会在波末自动收集，不构成提前拾取的边际收益。

`OpportunityPricingModel` 统一把地面材料、地面消耗品、实体死亡奖励和树木保留后果换算为材料等价边际
价值。实体死亡奖励同时包含概率掉落和确定属性变化：`DeathRewardProbabilityModel` 在规划快照上组合
材料的波次与潮汐波规则、消耗品和箱子的基础概率及当前幸运；`StatOpportunityPricingModel` 按当前属性、
机会曲线和剩余波次等价数计算确定属性变化的边际价值。有限轮次对局中的持久变化会在后续波次反复影响
事件机会，不能压缩成整局仅有一次机会；无尽模式没有有限未来范围，因此使用有界的波次等价量。具体
基准和无尽模式代理见 [仍需校准的假设](model-calibration.md#仍需校准的假设)。材料条件数量和概率保持
分离；树木收益中的材料使用与地面材料相同的本波兑现价格。
敌人材料存在击杀距离曲线时，完成账本先按当前敌我距离形成导航使用的目标价值，局部武器结果再按候选
路径采样点的预计敌我距离重估材料部分。曲线不缩放箱子、确定属性变化或中立单位奖励；这些收益继续按
各自机制结算。由此产生的是动作条件价值，而不是角色专属移动偏好。
箱子的道具选择价值由主线程知识适配层根据原版波次稀有度分布、当前已解锁道具池和玩家价格修正形成，
不消耗未来随机数。死亡奖励中尚未生成的恢复型消耗品按补充库存的边际价值计价；可治疗箱子的道具选择
与恢复描述同一个箱子结果的不同收益，不是重复计算。因此树木因较高箱率自然具有高价值，生命越低时
恢复收益还会连续增值，无需增加“优先打树”或“低血找树”的策略分支。

`EnemyCompletionValueModel` 在每次规划中一次性建立敌人完成状态转移账本。奖励变化通道记录敌人死亡奖励
减去失去的存活奖励；存活负担解除通道记录直接接触与远程压力、来源死亡可清除的当前投射物、存活期间预计
生成的敌群，以及强化和治疗现存敌群造成的负担。治疗机会以单个目标当前缺失生命和单次治疗量为上限，
再按触发区间隙与双方特征运动距离衰减；满血目标不贡献治疗负担。敌人可给玩家提供的治疗机会采用同一触发区
和距离语义。死亡触发生成的敌群进入死亡后果通道。波末保留敌人的
收益及其可给玩家提供的治疗机会分别使前两个通道减小，必要时可使通道为负。所有项先换成材料等价值；
局部武器结果预测按离散击打工作量形成的完成份额兑现三个通道；统一目标投影再把该账本与树木的零负担、
零死亡后果和破坏收益放入同一导航机会通道，并乘共享攻击容量下的完成份额。两者都是受容量约束的期望代理，
不宣称统计概率。账本构造只扫描敌群一次，动作、导航和武器结果预测共享该账本，避免每个消费者重复计算。

效用模型对 `integrated_environmental_exposure`、由 `forecast_expected_health_loss` 换算的
`forecast_health_inventory_loss_value`，以及 `forecast_terminal_health_risk` 按当前对局延续价值换算的
`expected_run_continuation_value_loss` 施加生存成本。位置域
`peak_path_collision_risk`、接触前视段峰值与 `unresolved_collision_risk` 取较大值后形成完整局部窗诊断；
默认局部接触窗内的几何机会与近端动作窗内的其他生命变化合并为条件生命成本输入，不重复计分。提交期
子集仍以 `committed_expected_health_loss` 保留，供短期执行诊断，不再作为独立字段或死亡概率重复计分。
独立队友的未知后续运动使用当前 TTC 风险；尚未
发生的冲撞使用目标分布和解析到达时间；具有受支持轨迹的实体只使用连续路径扫掠。只有提交期内确定且
存在替代动作的终止碰撞构成
[决策流程](#决策流程)第 8 步的执行资格。该边界只阻止已经不可撤销的碰撞终止转移，不指定逃离、接敌、
拾取或巡航方向。

环境暴露变化率和两类碰撞证据只保留在诊断账本中。敌人轨迹的位置不确定半径
只扩大邻近压力，不扩大任何接触碰撞圆；伤害碰撞几何始终使用由玩家、敌人和投射物的标准碰撞形状
转换成的接触包围圆。规划层不推断、修补或替换这些必需字段，避免把精灵留白或“可能位于某处”误写成
“TTC 为零”。

友方作用区进入公共效用账本。炮塔和战斗宠物只有在
预测敌人进入其作用范围且仍对玩家形成近身压力时才产生减压价值；猫炮还要求玩家进入其可见启用范围。
地雷按一次性机会计分：原版踏板同时接受玩家与敌人，因此两者的预测路径都可消耗储备；只有当前可见
敌人进入爆炸覆盖时才兑现伤害价值，离开视野的记忆轨迹只形成环境压力。治疗覆盖随缺失生命提高权重。
水母盾只在预测会先于玩家截获威胁弹道时计分。
原版玩家和敌人的碰撞层不与构筑物碰撞；构筑物不参与实体掩体计算。

### 武器结果预测

`WeaponOutcomeForecastModel` 读取玩家自身武器的 `timing`、`delivery`、`impact` 和 `rules`，估计动作预测窗内
自动攻击对候选结果的期望修正。当前可见目标的中心距离按原版精确锁定边界准入，再按原版最近目标规则
分配主目标；目标外观半径只用于命中路径交会，不能增加锁定可用性。较远目标只有在所有更近目标不可用时
才获得主目标。锁定资格只决定能否开始攻击；共享的武器路径接触模型再以最大传播距离、走廊宽度和目标
外观半径计算实际主路径接触份额。局部伤害、命中、完成价值及命中后规则共用该份额，因此原版近战
额外锁定余量不会被误写成可命中距离；导航只使用各武器锁定区间及其目标相关容量份额判断射程访问，
不预支路径命中。结果预测沿
候选路径的起点、中点与终点积分，因而接近目标只从实际形成攻击窗口之后产生输出，不把终点覆盖预付给
整段动作。主目标的期望分布确定攻击方向；直接贯穿只累计几何上落在路径角覆盖、走廊宽度和最大距离内
的其他目标，弹射与命中后
投送再按覆盖目标质量、投送容量与伤害保留率折成期望乘数。主目标、直接路径次要目标和重定向目标都按
目标声明的完成机制归属命中容量，因此落在树木上的次要路径不会被记入敌人命中或敌人伤害。模型不构造
逐发、逐弹体、逐碰撞或逐事件传播图。

近战路径保留攻击形态差异：刺击以朝向目标的直线走廊结算，扫击以原版角范围和最大传播距离形成扇区
近似。候选移动改变接触时的相对位置后，会改变主目标接触与额外目标容量，并通过既有结果结算影响预计
伤害、生命偷取和目标完成价值。因此，均能命中主目标的两个方案仍可能因额外覆盖而具有不同收益；单一
目标已经稳定处于路径内时，进一步贴近不会虚构额外伤害。原版角度与 hitbox 时序证据见
[原版玩家效果与武器机制审计](vanilla-item-weapon-mechanics.md#近战攻击时序与路径)。

每个保留动作直接评价自身路径，避免空间插值抹平最近目标的 Voronoi 边界；这使细微位移造成的树木与敌人
目标归属变化能够进入效用账本。同一物理帧内仍复用目标运动预测与按移动状态投影的武器模型。每个路径
采样点只排序一次最近目标；锁定范围与直接路径几何相同的武器共享覆盖和贯穿交会，再分别应用各自时序、
伤害、暴击、生命偷取与规则容量。完全可用的近目标会使后续目标的主目标选择权重归零；这些目标不再
展开直接路径交会，因为它们对期望值的贡献严格为零。其余目标仍使用相同的密集目标语义。

知识编译器把玩家自身当前可感知的剩余冷却与已显现攻击相位合成为下一次攻击就绪时间，并单独保留当前
攻击阶段的结束时刻。原版在攻击动画期间冻结冷却，因此进行中的攻击还要计入由稳定动画机制和当前相位
得到的剩余时长。相位起点按观察物理帧记录，误差不超过一个观察周期；两项当前事实都不由长期均值替代。
首次机会之后才使用含额外长装填的长期期望周期，不读取下一次尚未揭示的随机冷却，也不展开逐发状态机。
因此，接近锁定边界的价值会随当前节奏自然变化，而不是由固定接战距离或开火策略触发。

已开始的近战攻击作为已提交接触单独预测。执行延迟投影先推进其接触时间窗，武器结果模型再在剩余有效
窗口的起点、中点与终点比较候选动作覆盖，并只采用期望命中数最高的单个截面；移动许可只约束
后续尚未开始的攻击。未直接完成目标的
已提交伤害，只有在共享波次预测仍支持完成该目标时，才按净完成价值形成进度价值，并扣除完成结算已经
兑现的份额。模型不区分同一接触阶段内各目标的命中先后，也不跟踪逐目标 hitbox 消费状态。

`WeaponAttackScheduleModel` 从统一规则语言筛出会改变武器时序的状态变换，再按候选路径上的材料拾取时刻
推进平均周期更新过程。冷却归零可以提前下一次就绪，但不能取消已经显现的攻击阶段。确定拾取按时序共同
结算；不确定权重只是各拾取物的存在置信度，并不证明事件相互独立，因此模型只采用证据最强的单个不确定
分支增量，不相乘也不叠加。无状态转移时仍使用连续的长期期望容量；新增候选拾取不能降低已有容量。该
模型只认识事件、时刻和时序目标，不认识角色、道具或武器 ID。

每个路径采样点先产生统一目标命中容量，再按 `target_id` 把敌人的命中和伤害累计为逐目标攻击工作，
并按目标的 `weapon_response` 解释完成机制。所有武器和路径采样点贡献完毕后，模型才用同一目标的
预测单击伤害与剩余生命计算离散所需命中数；预测容量不能到达最后一次命中时，该目标不兑现完成价值。
动作条件死亡奖励随同一目标在各路径采样点的预计命中质量累计，因此结算使用的预计击杀距离不会在目标
之间交换，也不会脱离能够完成该目标的攻击工作单独计分。
树木同时比较伤害耗尽剩余生命和玩家命中达到剩余命中上限两条路径，采用先完成者；一击树效果把玩家
命中需求降为一次。

`WeaponOutcomeSettlementModel` 统一结算上述攻击工作：每个敌人的完成量不超过一次，计入的伤害不超过
该敌人的剩余生命，树木价值不超过树木总收获价值。奖励变化、存活负担解除、死亡后果和击杀权重均取自
完成量所属的目标；武器归属的击杀成长再按同一目标上的期望命中贡献分配。目标之间不能交换生命、命中
容量、价值或成长进度。

### 计算预算与搜索工作分配

计算预算以实测物理回调峰值和规划成本为输入。`PhysicsFrameBudgetMonitor` 读取
`Performance.TIME_PHYSICS_PROCESS`；该字段的单位是单个物理帧耗时，Godot 3 在报告窗口内累计最大回调
耗时后发布监视值。控制层在同一 `idle frame` 内只推进一次估计，不按回调数归一；监视器以 `0.5 s`
时间常数平滑已发布峰值，预算直接使用该估计，不另加偏差项。物理帧容量由
`Engine.iterations_per_second` 派生。字段单位见
[Godot 3.5 Performance 文档](https://docs.godotengine.org/en/3.5/classes/class_performance.html#enum-performance-monitor)，
报告窗口的实现见
[Godot 3.x 主循环](https://github.com/godotengine/godot/blob/6ac964295fe9ed4b4e5d14ea4f732223a00a0ac6/main/main.cpp#L2337-L2542)：

```text
单个规划环中单个玩家的规划耗时预算
= min(控制窗长度 × 0.5, max(0, 物理帧容量 - 物理回调峰值 EMA) × 控制窗物理帧数)
  × 本规划环容量份额 / 调度规划器数
```

战术环使用三个物理 tick 的窗口和 `0.8` 容量份额；战略环使用十二个物理 tick 的窗口和 `0.2` 容量份额。
两者的平均容量之和仍受同一个“半窗”后台上限约束，而战术环得到更高份额和更短的结果新鲜度目标。
`TacticalMovementPlanner` 与 `StrategicNavigationPlanner` 分别用 `OS.get_ticks_usec()` 测量本线程主要规划路径。
`PlanningComputeBudgetPolicy` 把控制窗内聚合帧余量转换成单调时钟截止，并根据近期规划耗时与轮询上界
估计规划开始后的动作生效延迟。存在有效帧样本且预算为正时，它将近期规划耗时占预算的比例平方并截断
为连续预算压力；尚无帧样本时，或预算为正但尚无规划耗时估计时，压力保持为零，不从未知状态推断过载；
实测预算为零时，压力饱和。对于搜索工作，它只负责时间准入：战略线程的额外导航评价与战术线程的局部
角度细分仅在预计单项耗时仍容纳于各自截止时启动。每个保留候选仍执行相同的动作路径武器、碰撞和
事件预测。计算压力不会切换结果语义，也不会删除几何基线动作。

动作角向覆盖由玩家碰撞半径和一个提交期的移动距离派生，并完整保留为局部均匀基线。近似战略导航的
均匀格点随预算压力在四至八个方向间收缩；机会聚合方向与后续角区间细分都属于预算内搜索，只有经过
完整轨迹评价后才可能成为胜出导航方向。`PlanningSearchWorkAllocator` 以此前规划耗时对当前控制窗余量
的连续压力计算细分保真度：`q = q_min ^ budget_pressure`；导航额外评价额度与移动角区间精炼额度从该
细分保真度派生。整数化只发生在离散工作额度和导航方向数边界。

时间采样由碰撞直径、控制期与相关曲线弹最大角频率推导，不随可选搜索额度降低。动作搜索从当轮均匀动作
格点与胜出导航方向形成可执行基线，再在优胜动作的相邻角区间递归插入
中点。导航与近场的额外工作分别受本线程曲线额度和截止约束；战略基线超时不会占用战术工作队列，战术
也不等待同一观察代际的战略结果。

`ProjectileReachabilityFilter` 在重复战术扫掠之前，用导航时域内玩家最大位移、可见投射物当前速度、
已观察加速度和已解析曲线速度的保守积分上界判断规划域可达性。不能影响局部可达域的投射物仍保留在
原始观察和遥测中，但不再提高局部时间采样数，也不参与每个动作的重复碰撞计算。敌人尚无适用于全部
规划消费者的统一最大机动能力契约，因此不会从规划观察中全局延后；武器结果预测只消费当前可见敌人的
短时运动估计，不把不可见目标或运动不确定域扩写成火力收益。

`projectile_filter` 公开过滤结果：`included_projectile_count` 与 `deferred_projectile_count` 分别记录进入
重复规划计算和被延后的投射物数；`horizon_seconds` 是判定使用的有效导航时域；`mode` 记录当前保守距离
判据。过滤后的观察只在规划包内部传递，不替换采样中的原始公共观察。

战术与战略规划器各自注入一个本次请求作用域的位置响应缓存，按“轨迹、预测时刻、候选玩家位移”在本线程
内部复用完整结果；两个线程不共享可变缓存，战略请求也不会污染战术代际。每个不同候选仍单独计算玩家
位移造成的位置响应反事实。武器结果预测按战术候选动作直接评价路径，不跨帧保留地图或目标状态。
`model.derived.enemy_position_response_cache` 记录目标位置响应预测的缓存命中数、未命中数与复用比例，
供性能复盘确认共享是否生效；这些诊断不参与预算或动作选择。

`compute_budget` 公开本轮分配和反馈状态：

| 字段 | 含义 |
| --- | --- |
| `budget_model`、`budget_pressure`、`has_deadline` | 当前使用的半控制窗上限帧余量预算模型、`[0, 1]` 预算压力，以及本轮是否有可用最终截止；压力通常由预测预算利用率平方得到，无帧样本或正预算下尚无规划成本估计时为 `0`，实测预算为零时为 `1` |
| `planning_started_usec`、`planning_deadline_usec` | 规划器实际计算起点与本轮计算的最终截止 |
| `request_queue_delay_usec` | 从控制器取得这批观察到当前规划器实际开始计算的排队时间；多人规划时包含前序玩家的计算 |
| `planning_deadline_overrun_usec` | 实际结束时超过最终截止的微秒数；无帧样本时为 `null` |
| `estimated_work_unit_duration_usec` | `navigation_evaluation` 与 `movement_refinement` 单次额外工作耗时的 EMA |
| `physics_frame_capacity_usec` | 由物理帧率派生的单帧容量 |
| `physics_process_peak_usec_ema` | Godot 报告窗口内最大物理回调耗时的指数移动平均 |
| `planning_window_usec`、`planning_window_physics_frames` | 一次重规划周期的墙钟长度和包含的物理帧数；单帧余量在这个后台计算窗口内聚合后才形成规划预算 |
| `planning_loop_capacity_share` | 当前规划环在共享后台容量中的份额；战术为 `0.8`，战略为 `0.2` |
| `has_frame_time_sample`、`scheduled_planner_count` | 帧耗时样本是否有效，以及共享余量的规划器数量 |
| `planning_duration_budget_usec` | 分配给单个规划器的本轮耗时预算 |
| `planning_duration_usec`、`planning_duration_usec_ema` | 本轮实测规划耗时及其指数移动平均 |
| `planning_turnaround_usec` | 从后台规划开始到主线程接收并准备应用结果的总周转时间，包含计算、线程调度和轮询等待 |
| `expected_post_start_actuation_delay_usec` | 分配时根据上一轮规划耗时 EMA 与一个物理帧轮询上界估计的规划开始后延迟；与已经发生的排队延迟共同形成动作生效时刻投影 |
| `predicted_planning_budget_utilization` | 分配时已知的规划耗时 EMA 与本轮预算之比；用于形成预算压力 |
| `planning_duration_budget_utilization` | 本轮实测耗时与耗时预算之比；大于 `1` 表示超出预算 |

战术 `search_work_allocation` 记录 `movement_refinement_limit`；战略
`strategic_navigation_guidance_diagnostics.search_work_allocation` 记录 `navigation_baseline_direction_count` 与
`navigation_extra_evaluation_limit`。两者分别保留 `allocation_model`、`budget_pressure`、受压力影响的
`refinement_fidelity` 及其下限，不向另一规划环公开无效额度。
局部几何方向基线另由 `model.derived.geometry_direction_count` 记录，不混入预算分配。该记录只解释搜索
计算，不改变碰撞时间采样，也不改变敌人、材料、消耗品、构筑物或风险本身的价值。预算压力只能削减
导航额外评价、动作角区间细分，并把近似战略价值场的均匀扫描从八向收缩至四向；不能删除由碰撞几何
派生的均匀动作格点。导航价值在保留样本间作环形插值，因此局部动作仍得到连续的延续价值证据。

`actuation_projection` 记录本轮使用的 `delay_seconds`、延迟期间继续生效的 `active_movement` 与由此产生的
`player_displacement`。延迟由已经发生的 `request_queue_delay_usec`、上一轮规划耗时 EMA 与一个物理帧
轮询上界组成。复盘弹道误差时还应与实测 `planning_turnaround_usec` 对照；投影延迟是规划开始时可用的
因果估计，周转时间是结果抵达后才知道的实测值。

动作基线在均匀方向之外只加入已经通过完整轨迹评价胜出的导航方向，并单独加入零移动输入。单步动作空间
使用确定性自适应角区间，不需要模拟退火；若以后允许多个自由动作段，组合数会指数增长，届时再考虑束
搜索或交叉熵方法。

两条后台规划线程都不在物理回调中执行；其 CPU 争用对主线程的影响会在后续报告的物理回调峰值中体现。
Godot 3 不提供该 `Thread` API 的线程优先级设置，因此响应优先级由独立邮箱、四倍调度频率和 `0.8/0.2`
容量分账实现，而不宣称操作系统级抢占优先级。单项耗时
EMA 只能阻止启动预计无法在截止前完成的额外工作，不能中断已经开始的单项计算；基线工作、首次成本
估计、同帧突发负载和监视值更新延迟仍可能造成超预算。预算为零或尚无帧样本时都不启动受截止控制的
额外工作；两者都保留完整几何动作基线，后者不凭未知状态降低细分保真度。

## 算法依据与适用边界

- Khatib 的人工势场提供了实时局部避障的运行成本思想，但普通吸引/排斥势场存在局部极小值问题，因此
  本实现不直接沿暴露梯度控制。
- Fiorini–Shiller 的 Velocity Obstacle 与原版“输入直接决定平移速度”的一阶动力学匹配，用于没有受支持
  未来轨迹的动态圆盘交会；玩家可主动承伤，所以实现采用 TTC 连续松弛而不是硬禁用碰撞锥内速度。已有
  解析轨迹的实体使用连续路径扫掠，不得进入该近似。
- 滚动时域控制负责组合短期运行成本、战斗收益和导航轨迹价值；离散动作搜索是受限预算的 MPC
  近似，不提供机器人控制意义上的稳定性或安全证明。
- 多速率分层控制把变化快、直接决定执行安全的战术动作与变化较慢的长时域价值场分开求解。战略输出采用
  带期限的零阶保持；它只作为战术效用的指导输入，不进入执行资格边界，因此丢失或延迟不会停止局部闭环。
- 可行域与控制余量的观点用于表达下一控制期的角向机动余量：可达敌人圆盘与已知地图边界转换为遮蔽
  角区间并取并集。边界不是硬禁区，聚团、包围和边界重叠的差异由剩余可用方向产生，而不是由独立
  边缘惩罚或角落倍率产生。
- 截止可达集的观点用于区分“目标会自行进入武器射程”和“必须由玩家运动创造射程访问”。目标在剩余
  波时内能够自主闭合的缺口先进入零输入反事实，只有剩余缺口形成动作边际价值；这是逐目标常数时间的
  可达性近似，不求解完整目标管或 Hamilton–Jacobi 方程。
- POMDP 的信念状态观点用于区分“已经观察到的世界”与“对当前世界的置信估计”。地图探索不再编码方向，
  而把候选视点带来的新观察和对陈旧覆盖的再观察作为后续决策价值；当前即时生存缓冲形成局部暴露单价，
  有限时域内可兑现的补充流形成补给库存影子价格。概率终止风险和预测窗后段风险仍与其他动作结果在
  同一效用账本交换；可避免的确定提交期死亡由执行资格单独处理。
- 动态随机背包的容量影子价格用于解释“现在消耗一单位可补充库存会放弃多少后续选择”；随机可达性则
  启发即时可生存性与期望性能分离。当前实现只是适合实时预算的确定性等价近似，不声称求解完整动态规划
  或提供概率安全保证。

主要参考：[Khatib, *Real-Time Obstacle Avoidance for Manipulators and Mobile Robots*
(1986)](https://khatib.stanford.edu/publications/pdfs/Khatib_1986_IJRR.pdf)；[Fiorini & Shiller,
*Motion Planning in Dynamic Environments Using Velocity Obstacles*
(1998)](https://doi.org/10.1177/027836499801700706)；[Kaelbling, Littman & Cassandra,
*Planning and Acting in Partially Observable Stochastic Domains*
(1998)](https://doi.org/10.1016/S0004-3702(98)00023-X)；[Chow et al.,
*Risk-Sensitive and Robust Decision-Making: a CVaR Optimization Approach*
(2015)](https://papers.nips.cc/paper_files/paper/2015/hash/64223ccf70bbb65a3a4aceac37e21016-Abstract.html)；
[Kleywegt & Papastavrou, *The Dynamic and Stochastic Knapsack Problem with Random Sized Items*
(2001)](https://doi.org/10.1287/opre.49.1.26.11185)；[Vinod & Oishi,
*Stochastic Reachability of a Target Tube: Theory and Computation*
(2021)](https://doi.org/10.1016/j.automatica.2020.109458)。完整 HJ reachability 可以
给出更强安全集合，但其状态维度和实时求解成本不适合当前逐玩家、逐控制期规划预算。
