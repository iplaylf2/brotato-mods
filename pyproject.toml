[project]
name = "brotato-mods-devtools"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
  "gdtoolkit==3.6.0",
  "ruff",
  # gdtoolkit 3 imports pkg_resources without declaring setuptools.
  "setuptools<81",
]

[tool.uv]
package = false
